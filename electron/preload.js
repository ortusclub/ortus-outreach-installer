// Empty preload — strict context isolation, no Node APIs exposed to the
// renderer. The web UI talks to the local server over HTTP exactly as in
// the browser dev workflow, so it needs nothing privileged here.
