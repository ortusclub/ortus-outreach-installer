---
phase: 03-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [elevenlabs-apps-script.js]
autonomous: true
requirements: [VOICE-05, VOICE-06, VOICE-07]

must_haves:
  truths:
    - "getVoiceList returns preview_url for each voice"
    - "getVoicePreview returns base64-encoded audio for a given voiceId"
    - "Bookmarked voices sort to the top of the voice list"
  artifacts:
    - path: "elevenlabs-apps-script.js"
      provides: "getVoicePreview function, updated getVoiceList with preview_url"
      contains: "getVoicePreview"
  key_links:
    - from: "getVoiceList"
      to: "ElevenLabs /v1/voices API"
      via: "UrlFetchApp.fetch"
      pattern: "preview_url"
    - from: "getVoicePreview"
      to: "voice preview_url"
      via: "UrlFetchApp.fetch + base64Encode"
      pattern: "Utilities.base64Encode"
---

<objective>
Add server-side support for voice previews and bookmark sorting in the Google Apps Script.

Purpose: The sidebar UI (Plan 02) needs server-side functions to proxy voice audio (CORS-blocked in GAS iframe) and needs `preview_url` data from the voice list. This plan adds those server capabilities.

Output: Updated `getVoiceList()` returning `preview_url` per voice, new `getVoicePreview(voiceId)` function, and user-managed bookmark persistence via UserProperties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-polish/03-CONTEXT.md
@.planning/phases/02-voice-selection-core/02-01-SUMMARY.md
@elevenlabs-apps-script.js

<interfaces>
<!-- Current getVoiceList at lines 678-712 returns: {voice_id, name, description, category} -->
<!-- Need to add: preview_url field to each voice object -->
<!-- Current voice loading section at lines 902-925 in sidebar JS consumes the voice list -->

From elevenlabs-apps-script.js (lines 678-712):
```javascript
function getVoiceList() {
  // Fetches /v1/voices, returns array of {voice_id, name, description, category}
  // description = concatenated labels (accent, gender, description)
  // Missing: preview_url, is_bookmarked
}
```

From elevenlabs-apps-script.js (lines 714-722):
```javascript
function getLastUsedVoiceId() { /* returns string from UserProperties */ }
function saveLastUsedVoiceId(voiceId) { /* saves to UserProperties */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preview_url to getVoiceList and create getVoicePreview proxy</name>
  <read_first>elevenlabs-apps-script.js (lines 678-712 for getVoiceList, lines 36 for CONFIG)</read_first>
  <files>elevenlabs-apps-script.js</files>
  <action>
Two changes in the Voice Selection section (after line 722):

**1. Update getVoiceList() (lines 695-707)** to include `preview_url` in the returned objects.

In the `.map(function(v) { ... })` block, add `preview_url: v.preview_url || ""` to the return object. The updated return should be:
```
return {
  voice_id: v.voice_id,
  name: v.name,
  description: labelParts.join(", "),
  category: v.category || "",
  preview_url: v.preview_url || ""
};
```

**2. Add a new function `getVoicePreview(voiceId)`** after `saveLastUsedVoiceId()` (after line 722).

This function:
- Calls `getVoiceList()` to find the voice with matching `voice_id`
- Extracts its `preview_url`
- If no preview_url found, throws Error "No preview available for this voice"
- Uses `UrlFetchApp.fetch(previewUrl)` to download the audio
- Returns `Utilities.base64Encode(response.getBlob().getBytes())`
- Wrap in try/catch, log errors with Logger.log

Per D-02: This server-side proxy is required because preview URLs (storage.googleapis.com) are CORS-blocked in the GAS sandboxed iframe.

CRITICAL CONSTRAINTS (per D-13, D-14):
- NO `//` comments anywhere in this code. Use `/* */` block comments only.
- All string literals use double quotes in production code (single quotes are fine in server-side GAS functions since they are NOT inside the HTML string).
  </action>
  <verify>
    <automated>grep -c "getVoicePreview" elevenlabs-apps-script.js | grep -q "[1-9]" && grep -c "preview_url" elevenlabs-apps-script.js | grep -q "[2-9]" && grep -c "base64Encode" elevenlabs-apps-script.js | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- getVoiceList return object contains `preview_url: v.preview_url || ""`
- getVoicePreview function exists, accepts voiceId parameter
- getVoicePreview fetches preview_url via UrlFetchApp and returns base64-encoded audio bytes
- No `//` comments in added code
  </acceptance_criteria>
  <done>getVoiceList returns preview_url, getVoicePreview proxies audio as base64</done>
</task>

<task type="auto">
  <name>Task 2: Add bookmark persistence functions (toggleVoiceBookmark, getBookmarkedVoiceIds) and sort bookmarks first in getVoiceList</name>
  <read_first>elevenlabs-apps-script.js (lines 714-722 for UserProperties pattern, lines 695-711 for getVoiceList return)</read_first>
  <files>elevenlabs-apps-script.js</files>
  <action>
Per D-07 and D-08: The ElevenLabs API `is_bookmarked` field is unreliable (was null in testing). Use local UserProperties for bookmark persistence instead.

**1. Add two new functions** after `getVoicePreview()`:

`getBookmarkedVoiceIds()`:
- Returns JSON.parse of `PropertiesService.getUserProperties().getProperty("BOOKMARKED_VOICES")` or empty array `[]`
- Wrap JSON.parse in try/catch, return `[]` on error

`toggleVoiceBookmark(voiceId)`:
- Gets current bookmarks via `getBookmarkedVoiceIds()`
- If voiceId is in the array, remove it (filter). If not, push it.
- Save back via `PropertiesService.getUserProperties().setProperty("BOOKMARKED_VOICES", JSON.stringify(bookmarks))`
- Return the updated bookmarks array

**2. Update getVoiceList()** to sort bookmarked voices first:
- After building the `voices` array (after the `.map()`), get bookmarks via `getBookmarkedVoiceIds()`
- Sort: voices with `voice_id` in bookmarks array come first, rest maintain original order
- Add `bookmarked: true/false` field to each voice object in the return

The sort logic:
```
var bookmarks = getBookmarkedVoiceIds();
voices.forEach(function(v) { v.bookmarked = bookmarks.indexOf(v.voice_id) !== -1; });
voices.sort(function(a, b) { return (b.bookmarked ? 1 : 0) - (a.bookmarked ? 1 : 0); });
```

CRITICAL: NO `//` comments. Use `/* */` only.
  </action>
  <verify>
    <automated>grep -c "getBookmarkedVoiceIds" elevenlabs-apps-script.js | grep -q "[2-9]" && grep -c "toggleVoiceBookmark" elevenlabs-apps-script.js | grep -q "[1-9]" && grep -c "BOOKMARKED_VOICES" elevenlabs-apps-script.js | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- getBookmarkedVoiceIds function exists, reads from UserProperties, returns array
- toggleVoiceBookmark function exists, adds/removes voiceId, persists to UserProperties
- getVoiceList sorts bookmarked voices to top of returned array
- Each voice object includes `bookmarked: true/false` field
- No `//` comments in added code
  </acceptance_criteria>
  <done>Bookmark functions work via UserProperties, getVoiceList returns sorted list with bookmarked flag</done>
</task>

</tasks>

<verification>
- `grep "getVoicePreview" elevenlabs-apps-script.js` shows the function
- `grep "preview_url" elevenlabs-apps-script.js` shows it in getVoiceList return AND in getVoicePreview
- `grep "getBookmarkedVoiceIds" elevenlabs-apps-script.js` shows the function
- `grep "toggleVoiceBookmark" elevenlabs-apps-script.js` shows the function
- `grep "BOOKMARKED_VOICES" elevenlabs-apps-script.js` shows UserProperties key
- `grep -c "//" elevenlabs-apps-script.js` should show 0 new `//` comment lines in the voice section
</verification>

<success_criteria>
- getVoiceList returns {voice_id, name, description, category, preview_url, bookmarked} sorted with bookmarked first
- getVoicePreview(voiceId) fetches audio and returns base64 string
- getBookmarkedVoiceIds() returns persisted bookmark array
- toggleVoiceBookmark(voiceId) toggles bookmark state and persists
- All four functions are callable via google.script.run from the sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-01-SUMMARY.md`
</output>
