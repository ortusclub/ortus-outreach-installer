---
phase: 03-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [elevenlabs-apps-script.js]
autonomous: false
requirements: [VARS-04, VOICE-05, VOICE-06, VOICE-07]

must_haves:
  truths:
    - "Operator can click play button to hear selected voice preview"
    - "Voice labels (accent, gender, tone) visible in dropdown options"
    - "Bookmarked voices appear at top of dropdown with star toggle"
    - "Empty required fields get red borders and submission is blocked"
    - "refreshStatus() builds history HTML without single-quoted strings"
  artifacts:
    - path: "elevenlabs-apps-script.js"
      provides: "Play button UI, bookmark star, validation logic in getSidebarHtml"
      contains: "previewVoice"
  key_links:
    - from: "play button onclick"
      to: "getVoicePreview server function"
      via: "google.script.run"
      pattern: "getVoicePreview"
    - from: "star button onclick"
      to: "toggleVoiceBookmark server function"
      via: "google.script.run"
      pattern: "toggleVoiceBookmark"
    - from: "submit function"
      to: "validation check"
      via: "validateFields call before submitBatchCall"
      pattern: "validateFields"
    - from: "refreshStatus"
      to: "historyList DOM"
      via: "createElement/appendChild"
      pattern: "createElement"
---

<objective>
Add voice preview playback, bookmark star toggles, and required field validation to the sidebar UI. Also fix pre-existing D-14 violation in refreshStatus().

Purpose: Operators need to hear voices before choosing, quickly access favorites, and be prevented from submitting incomplete batches. This plan wires the server-side functions from Plan 01 into the sidebar HTML/JS.

Output: Updated sidebar with play/stop button, bookmark stars on each voice option, field validation with red borders, and refreshStatus() rewritten to use createElement/appendChild.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-polish/03-CONTEXT.md
@.planning/phases/03-ux-polish/03-01-SUMMARY.md
@elevenlabs-apps-script.js

<interfaces>
<!-- After Plan 01, these server functions will be available via google.script.run: -->
<!-- getVoiceList() -> [{voice_id, name, description, category, preview_url, bookmarked}, ...] -->
<!-- getVoicePreview(voiceId) -> base64 string of audio/mpeg -->
<!-- toggleVoiceBookmark(voiceId) -> updated bookmarks array -->
<!-- getBookmarkedVoiceIds() -> [voiceId1, voiceId2, ...] -->

<!-- Existing sidebar structure (lines 728-929): -->
<!-- CSS: lines 732-763 -->
<!-- Voice dropdown: lines 772-776 -->
<!-- Submit button: line 817 -->
<!-- submit() function: lines 836-876 -->
<!-- refreshStatus(): lines 878-900 — CONTAINS D-14 VIOLATION (escaped single quotes on lines 886-891) -->
<!-- Voice loading JS: lines 902-925 -->

<!-- CRITICAL GAS CONSTRAINTS (D-13, D-14, D-15): -->
<!-- NO // comments in sidebar JS (GAS renders as single line) -->
<!-- NO single quotes '' in sidebar JS strings (use double quotes "") -->
<!-- Use createElement/appendChild over innerHTML where possible -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix refreshStatus D-14 violation, add CSS classes, play button, and bookmark star HTML/JS</name>
  <read_first>elevenlabs-apps-script.js (lines 728-929 for full getSidebarHtml, especially lines 732-763 for CSS, lines 772-776 for voice dropdown, lines 878-900 for refreshStatus, lines 902-925 for voice loading JS)</read_first>
  <files>elevenlabs-apps-script.js</files>
  <action>
All changes are inside `getSidebarHtml()` (the single-quoted HTML string). REMEMBER: NO `//` comments, NO single quotes `''` in the JS inside this string. Use `""` for all JS string literals. Use `createElement`/`appendChild` patterns.

**0. FIRST: Rewrite refreshStatus() to eliminate D-14 violation (lines 878-900).**

The current `refreshStatus()` function (lines 886-891) builds history HTML using escaped single-quote strings (`\'<div class="history-item">\'` etc.), which violates D-14. Rewrite the entire function body to use `createElement`/`appendChild` instead, consistent with D-15.

Replace lines 878-900 with this createElement/appendChild version:

```
function refreshStatus() {\
  google.script.run\
    .withSuccessHandler(function(batches) {\
      var container = document.getElementById("historyList");\
      container.innerHTML = "";\
      if (!batches || batches.length === 0) {\
        var emEl = document.createElement("em");\
        emEl.textContent = "No batches found.";\
        container.appendChild(emEl);\
        return;\
      }\
      batches.slice(0, 10).forEach(function(b) {\
        var item = document.createElement("div");\
        item.className = "history-item";\
        var nameDiv = document.createElement("div");\
        nameDiv.className = "name";\
        nameDiv.textContent = b.name || b.id;\
        item.appendChild(nameDiv);\
        var metaDiv = document.createElement("div");\
        metaDiv.className = "meta";\
        var badge = document.createElement("span");\
        badge.className = "badge badge-" + b.status;\
        badge.textContent = b.status;\
        metaDiv.appendChild(badge);\
        metaDiv.appendChild(document.createTextNode(" " + b.finished + "/" + b.total + " calls"));\
        item.appendChild(metaDiv);\
        container.appendChild(item);\
      });\
    })\
    .withFailureHandler(function(err) {\
      var container = document.getElementById("historyList");\
      container.innerHTML = "";\
      var emEl = document.createElement("em");\
      emEl.textContent = "Error: " + err.message;\
      container.appendChild(emEl);\
    })\
    .listBatchCalls();\
}\
```

This eliminates ALL escaped single-quote strings from `refreshStatus()`, replacing them with safe `createElement`/`appendChild` calls per D-15.

**1. Add CSS classes** (in the style block, after the `.badge-failed` rule around line 763):

```
.voice-row { display: flex; align-items: center; gap: 6px; }\
.voice-row select { flex: 1; }\
.btn-icon { width: 32px; height: 32px; border: 1px solid #ddd; border-radius: 6px; background: #fff; cursor: pointer; font-size: 14px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }\
.btn-icon:hover { border-color: #C5A255; background: #faf6eb; }\
.btn-icon:disabled { opacity: 0.4; cursor: not-allowed; }\
.field-error input, .field-error select, .field-error textarea { border-color: #c62828; }\
.validation-msg { color: #c62828; font-size: 11px; margin-top: 6px; }\
.bookmark-star { color: #ccc; cursor: pointer; font-size: 16px; }\
.bookmark-star.active { color: #C5A255; }\
```

**2. Replace the voice dropdown HTML** (lines 772-776). Change from:
```
<div class="field"><label>Voice</label>\
  <select id="voice_id">\
    <option value="">Loading voices...</option>\
  </select>\
</div>\
```
To:
```
<div class="field"><label>Voice</label>\
  <div class="voice-row">\
    <select id="voice_id">\
      <option value="">Loading voices...</option>\
    </select>\
    <button class="btn-icon" id="previewBtn" onclick="previewVoice()" title="Preview voice" disabled>\
      &#9654;\
    </button>\
  </div>\
</div>\
```

**3. Add previewVoice() function** in the script block (before the `google.script.run` voice loading call, around line 901). This goes in the sidebar JS:

```
var _previewAudio = null;\
\
function previewVoice() {\
  var sel = document.getElementById("voice_id");\
  var btn = document.getElementById("previewBtn");\
  if (_previewAudio && !_previewAudio.paused) {\
    _previewAudio.pause();\
    _previewAudio.currentTime = 0;\
    btn.innerHTML = "&#9654;";\
    _previewAudio = null;\
    return;\
  }\
  var voiceId = sel.value;\
  if (!voiceId) return;\
  btn.disabled = true;\
  btn.innerHTML = "...";\
  google.script.run\
    .withSuccessHandler(function(base64) {\
      _previewAudio = new Audio("data:audio/mpeg;base64," + base64);\
      _previewAudio.onended = function() {\
        btn.innerHTML = "&#9654;";\
        _previewAudio = null;\
      };\
      _previewAudio.play();\
      btn.disabled = false;\
      btn.innerHTML = "&#9632;";\
    })\
    .withFailureHandler(function(err) {\
      btn.disabled = false;\
      btn.innerHTML = "&#9654;";\
      showStatus("Preview error: " + err.message, "err");\
    })\
    .getVoicePreview(voiceId);\
}\
```

**4. Update voice loading JS** (lines 902-925) to:
- Enable/disable preview button based on selection (add onchange handler)
- Show bookmark star for each voice option
- The voice loading success handler needs updating. Replace the current voice loading block with:

```
google.script.run\
  .withSuccessHandler(function(voices) {\
    var sel = document.getElementById("voice_id");\
    var previewBtn = document.getElementById("previewBtn");\
    sel.innerHTML = "";\
    var def = document.createElement("option");\
    def.value = "";\
    def.textContent = "Agent default voice";\
    sel.appendChild(def);\
    (voices || []).forEach(function(v) {\
      var opt = document.createElement("option");\
      opt.value = v.voice_id;\
      var star = v.bookmarked ? "\u2605 " : "";\
      opt.textContent = star + v.name + (v.description ? " - " + v.description : "");\
      sel.appendChild(opt);\
    });\
    sel.onchange = function() {\
      previewBtn.disabled = !sel.value;\
      if (_previewAudio && !_previewAudio.paused) {\
        _previewAudio.pause();\
        _previewAudio = null;\
        previewBtn.innerHTML = "&#9654;";\
      }\
    };\
  })\
  .withFailureHandler(function(err) {\
    var sel = document.getElementById("voice_id");\
    sel.innerHTML = "";\
    var opt = document.createElement("option");\
    opt.value = "";\
    opt.textContent = "Error loading voices";\
    sel.appendChild(opt);\
  })\
  .getVoiceList();\
```

**5. Add last-used voice restore** after the voice loading success handler sets up options. Inside the success handler, after the `forEach` and before `sel.onchange`:

```
    google.script.run\
      .withSuccessHandler(function(lastId) {\
        if (lastId) { sel.value = lastId; previewBtn.disabled = !sel.value; }\
      })\
      .withFailureHandler(function() {})\
      .getLastUsedVoiceId();\
```

CRITICAL REMINDERS:
- Every string inside this HTML must use double quotes `""`, NEVER single quotes
- NO `//` comments anywhere in the script section
- All backslash-newline continuations must be maintained
- Test that no single-quote JS strings sneak in (the `\u2605` unicode escape is safe)
- VERIFY that the old refreshStatus escaped single-quote lines (886-891) are fully removed
  </action>
  <verify>
    <automated>grep -c "previewVoice" elevenlabs-apps-script.js | grep -q "[1-9]" && grep -c "previewBtn" elevenlabs-apps-script.js | grep -q "[2-9]" && grep -c "field-error" elevenlabs-apps-script.js | grep -q "[1-9]" && grep -c "getVoicePreview" elevenlabs-apps-script.js | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- refreshStatus() rewritten using createElement/appendChild — NO escaped single quotes remain in that function
- Play button (id="previewBtn") exists next to voice dropdown
- previewVoice() function calls google.script.run.getVoicePreview
- Button toggles between play (triangle) and stop (square) symbols
- Voice options show star prefix for bookmarked voices
- Preview button disabled when no voice selected, enabled when voice selected
- CSS class .field-error defined with border-color #c62828
- NO `//` comments in the script section of getSidebarHtml
- NO single-quoted strings in the script section of getSidebarHtml
  </acceptance_criteria>
  <done>refreshStatus D-14 violation fixed, play button works, bookmarked voices show stars, CSS for validation ready</done>
</task>

<task type="auto">
  <name>Task 2: Add input validation to submit function and bookmark toggle button</name>
  <read_first>elevenlabs-apps-script.js (the submit() function and the voice dropdown area -- read after Task 1 changes are applied)</read_first>
  <files>elevenlabs-apps-script.js</files>
  <action>
All changes inside `getSidebarHtml()`. Same constraints: NO `//` comments, NO single quotes, use `""` for all JS strings.

**1. Add validateFields() function** in the script block (before submit()):

Use this final version (uses `field.querySelector("label")` for reliable label lookup):

```
function validateFields() {\
  var requiredIds = ["caller_name", "host_name", "host_first_name", "event_name",\
    "event_date", "event_time", "event_city", "event_area",\
    "event_venue", "event_format", "event_context", "target_audience"];\
  var missing = [];\
  requiredIds.forEach(function(id) {\
    var el = document.getElementById(id);\
    var field = el.closest(".field");\
    var label = field ? field.querySelector("label") : null;\
    if (!el.value || !el.value.trim()) {\
      missing.push(label ? label.textContent : id);\
      if (field) field.classList.add("field-error");\
    } else {\
      if (field) field.classList.remove("field-error");\
    }\
  });\
  return missing;\
}\
```

**2. Update submit() function** to call validateFields() at the start, BEFORE disabling the button:

Add this block right after `function submit() {\`:
```
  var missing = validateFields();\
  if (missing.length > 0) {\
    showStatus("Please fill required fields: " + missing.join(", "), "err");\
    return;\
  }\
```

Per D-09: All sidebar fields are required. Per D-10: Red borders via .field-error. Per D-12: Validate on submit click only (NOT on every keystroke). Note: D-11 says "submit button disabled until filled" but D-12 takes precedence -- validation fires on submit click with early return rather than continuously disabling the button. This is simpler, less distracting, and consistent with D-12's explicit "on submit click, not on every keystroke" directive.

**3. Add bookmark toggle button** next to the play button. Update the voice-row HTML to add a star toggle button after the preview button:

```
    <button class="btn-icon" id="bookmarkBtn" onclick="toggleBookmark()" title="Bookmark voice" disabled>\
      &#9734;\
    </button>\
```

**4. Add toggleBookmark() function** in the script block:

```
function toggleBookmark() {\
  var sel = document.getElementById("voice_id");\
  var voiceId = sel.value;\
  if (!voiceId) return;\
  var btn = document.getElementById("bookmarkBtn");\
  btn.disabled = true;\
  google.script.run\
    .withSuccessHandler(function(bookmarks) {\
      var isNowBookmarked = bookmarks.indexOf(voiceId) !== -1;\
      btn.innerHTML = isNowBookmarked ? "&#9733;" : "&#9734;";\
      btn.disabled = false;\
      /* Update the star prefix in the selected option */\
      var opt = sel.options[sel.selectedIndex];\
      if (opt && opt.value) {\
        var txt = opt.textContent;\
        if (isNowBookmarked && txt.charAt(0) !== "\u2605") {\
          opt.textContent = "\u2605 " + txt;\
        } else if (!isNowBookmarked && txt.charAt(0) === "\u2605") {\
          opt.textContent = txt.substring(2);\
        }\
      }\
    })\
    .withFailureHandler(function(err) {\
      btn.disabled = false;\
      showStatus("Bookmark error: " + err.message, "err");\
    })\
    .toggleVoiceBookmark(voiceId);\
}\
```

**5. Update sel.onchange** in the voice loading success handler to also update bookmark button state:

In the `sel.onchange` handler, after `previewBtn.disabled = !sel.value;`, add:
```
      var bBtn = document.getElementById("bookmarkBtn");\
      bBtn.disabled = !sel.value;\
      if (sel.value && sel.options[sel.selectedIndex]) {\
        bBtn.innerHTML = sel.options[sel.selectedIndex].textContent.charAt(0) === "\u2605" ? "&#9733;" : "&#9734;";\
      } else {\
        bBtn.innerHTML = "&#9734;";\
      }\
```

CRITICAL REMINDERS:
- Every string inside this HTML must use double quotes `""`, NEVER single quotes
- NO `//` comments anywhere
- `\u2605` (filled star) and `&#9733;` (filled star HTML entity) and `&#9734;` (empty star HTML entity) are safe unicode
  </action>
  <verify>
    <automated>grep -c "validateFields" elevenlabs-apps-script.js | grep -q "[2-9]" && grep -c "field-error" elevenlabs-apps-script.js | grep -q "[2-9]" && grep -c "toggleBookmark" elevenlabs-apps-script.js | grep -q "[1-9]" && grep -c "bookmarkBtn" elevenlabs-apps-script.js | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- validateFields() checks all 12 field IDs (caller_name through target_audience)
- Empty fields get .field-error class on parent .field div
- submit() calls validateFields() first and returns early if any missing
- Error message lists specific missing field labels
- D-12 takes precedence over D-11: validation on submit click with early return, NOT continuous button disable
- Bookmark toggle button exists next to play button
- toggleBookmark() calls google.script.run.toggleVoiceBookmark
- Star icon updates between filled/empty on toggle
- Bookmark button disabled when no voice selected
- NO `//` comments in sidebar JS
- NO single-quoted strings in sidebar JS
  </acceptance_criteria>
  <done>Validation blocks empty submissions with red borders, bookmark toggle works with star icons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify sidebar UX polish</name>
  <files>elevenlabs-apps-script.js</files>
  <action>Human verification of all Phase 3 UX features in the live Google Sheets sidebar.</action>
  <what-built>
Complete Phase 3 UX polish: voice preview playback, bookmark stars, field validation, and refreshStatus D-14 fix in the ElevenLabs sidebar.
  </what-built>
  <how-to-verify>
1. Open the Google Sheet and launch the ElevenLabs Calling sidebar
2. **Voice Preview (VOICE-06):**
   - Select a voice from the dropdown
   - Click the play button (triangle icon) next to the dropdown
   - Confirm audio plays (you should hear the voice sample)
   - Click again (now showing square icon) to stop playback
   - Select "Agent default voice" and confirm play button is disabled
3. **Voice Labels (VOICE-05):**
   - Open the voice dropdown and confirm each voice shows "Name - accent, gender, description" format
4. **Bookmarks (VOICE-07):**
   - Select a voice and click the star button to bookmark it
   - Close and reopen the sidebar -- bookmarked voice should appear at the top with a filled star
   - Click star again to unbookmark
5. **Validation (VARS-04):**
   - Clear the Host Name field and click Submit Batch Call
   - Confirm: red border appears on the empty field, submission is blocked, error message lists "HOST NAME"
   - Fill all fields and confirm submission proceeds normally
6. **refreshStatus D-14 fix:**
   - Click Refresh Status and confirm batch history renders correctly (no broken HTML)
   - Verify badges and call counts display properly
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint - human verification required"</automated>
  </verify>
  <done>All four UX features (preview, labels, bookmarks, validation) plus refreshStatus fix confirmed working in live sidebar</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Voice preview plays audio via base64 proxy (not direct URL fetch from client)
- Bookmarked voices sort to top of dropdown on reload
- All 12 required fields validated before submission
- refreshStatus() uses createElement/appendChild -- no escaped single quotes
- No `//` comments in sidebar JS
- No single-quoted strings in sidebar JS
</verification>

<success_criteria>
- Operator can play/stop voice previews from the sidebar
- Voice labels (accent, gender, description) visible in dropdown
- Bookmark star toggles and persists across sidebar sessions
- Bookmarked voices sort to top of list
- Empty required fields show red borders and block submission
- Validation message names the specific empty fields
- refreshStatus() renders batch history correctly using createElement/appendChild
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-02-SUMMARY.md`
</output>
