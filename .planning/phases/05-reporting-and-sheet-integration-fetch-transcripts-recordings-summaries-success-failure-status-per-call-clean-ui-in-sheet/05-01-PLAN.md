---
phase: 05-reporting-and-sheet-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - elevenlabs-apps-script.js
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
must_haves:
  truths:
    - "Completed calls have Outcome, Call Type, Summary, Transcript, Recording, Follow Up, Callback, Email Confirmed, and Seen Invite columns populated"
    - "Transcript is formatted as multi-line User/Agent turns in a single cell"
    - "Recording column contains a clickable hyperlink to the conversation audio endpoint"
    - "Data collection results (outcome, call_type, follow_up, callback, email_confirmed, has_seen_invite) are extracted from API response and written to correct columns"
  artifacts:
    - path: "elevenlabs-apps-script.js"
      provides: "Extended CALL_TRACKING_COLUMNS, enhanced updateSheetWithCallResults, getConversationDetail, formatTranscript, buildRecordingUrl, extractDataCollection"
      contains: "Outcome"
  key_links:
    - from: "updateSheetWithCallResults"
      to: "elevenlabsGet('/convai/conversations/' + conversationId)"
      via: "per-call detail fetch"
      pattern: "convai/conversations/"
    - from: "updateSheetWithCallResults"
      to: "sheet.getRange"
      via: "setValue for each new column"
      pattern: "getRange.*setValue"
---

<objective>
Extend the Apps Script to fetch full post-call conversation details from ElevenLabs and write 9 new data columns to the Google Sheet for every completed call.

Purpose: Operators need call outcomes, transcripts, summaries, recordings, and data collection results directly in the sheet without leaving Google Sheets or opening the ElevenLabs dashboard.

Output: Enhanced `elevenlabs-apps-script.js` with 9 new tracking columns populated automatically when `pollBatchStatus` triggers `updateSheetWithCallResults`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reporting-and-sheet-integration-fetch-transcripts-recordings-summaries-success-failure-status-per-call-clean-ui-in-sheet/05-CONTEXT.md

<interfaces>
<!-- Existing functions in elevenlabs-apps-script.js that this plan modifies or uses -->

From elevenlabs-apps-script.js:
```javascript
// Line 40-46 — CALL_TRACKING_COLUMNS to extend
var CALL_TRACKING_COLUMNS = [
  'Call Status', 'Call Date', 'Call Duration', 'Call Batch ID', 'Call Notes'
];

// Line 326 — Main function to enhance
function updateSheetWithCallResults(batchId, apiKey) { ... }

// Line 542 — API GET helper (prepends /v1 via CONFIG.API_BASE)
function elevenlabsGet(endpoint, apiKey) { ... }

// Line 566 — Sheet header reader
function getHeaders(sheet) { ... }

// Line 574 — Column finder
function findColumnIndex(headers, candidates) { ... }

// Line 595 — Column creator
function ensureCallColumnsOnSheet(sheet, headers) { ... }

// Line 381 — Status mapper
function mapCallStatus(status) { ... }
```

ElevenLabs API response shape for GET /v1/convai/conversations/{id}:
```json
{
  "conversation_id": "conv_xxx",
  "status": "done",
  "metadata": {
    "call_duration_secs": 45,
    "termination_reason": "agent_ended"
  },
  "analysis": {
    "transcript_summary": "The agent called John about...",
    "call_successful": "true",
    "data_collection_results": {
      "outcome": { "value": "Interested", "rationale": "..." },
      "call_type": { "value": "Spoke to Human", "rationale": "..." },
      "follow_up_action": { "value": "resend", "rationale": "..." },
      "callback_requested": { "value": "true", "rationale": "..." },
      "callback_when": { "value": "next Tuesday", "rationale": "..." },
      "prospect_email_confirmed": { "value": "john@example.com", "rationale": "..." },
      "has_seen_invite": { "value": "true", "rationale": "..." }
    }
  },
  "transcript": [
    { "role": "agent", "message": "Hi, this is Sarah calling...", "time_in_call_secs": 0.5 },
    { "role": "user", "message": "Hello?", "time_in_call_secs": 3.2 }
  ],
  "has_audio": true
}
```
</interfaces>

CRITICAL GAS CONSTRAINTS (from Phase 2 lessons):
1. NO // comments in sidebar JS inside getSidebarHtml() — GAS renders as single line
2. NO single quotes inside GAS HTML string — use double quotes
3. These constraints do NOT apply to server-side functions (this plan is all server-side)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CALL_TRACKING_COLUMNS and add helper functions</name>
  <files>elevenlabs-apps-script.js</files>
  <read_first>elevenlabs-apps-script.js lines 40-46 (CALL_TRACKING_COLUMNS), lines 326-379 (updateSheetWithCallResults), lines 542-560 (elevenlabsGet), lines 595-609 (ensureCallColumnsOnSheet)</read_first>
  <action>
1. Extend the CALL_TRACKING_COLUMNS array (line 40) to add 9 new columns after the existing 5. The full array must be:
   ```
   var CALL_TRACKING_COLUMNS = [
     'Call Status', 'Call Date', 'Call Duration', 'Call Batch ID', 'Call Notes',
     'Outcome', 'Call Type', 'Summary', 'Transcript', 'Recording',
     'Follow Up', 'Callback', 'Email Confirmed', 'Seen Invite'
   ];
   ```

2. Add a `getConversationDetail(conversationId, apiKey)` function that:
   - Calls `elevenlabsGet('/convai/conversations/' + conversationId, apiKey)`
   - Returns the full response object (transcript, analysis, metadata, has_audio)
   - Wraps in try/catch, returns null on failure with Logger.log

3. Add a `formatTranscript(transcriptArray)` function that:
   - Takes the `transcript` array from the API response (array of `{role, message, time_in_call_secs}`)
   - Returns a single string with each turn on a new line: `"Agent: Hi, this is Sarah...\nUser: Hello?"`
   - Capitalizes role: "agent" -> "Agent", "user" -> "User"
   - Returns empty string if transcriptArray is falsy or empty

4. Add a `buildRecordingUrl(conversationId)` function that:
   - Returns `CONFIG.API_BASE + '/convai/conversations/' + conversationId + '/audio'`
   - This URL requires the API key header, so we will create a hyperlink with a label "(requires API key)" per D-04/D-11

5. Add an `extractDataCollection(analysis)` function that:
   - Takes the `analysis` object from conversation detail
   - Extracts `data_collection_results` (a dict where each key has `{value, rationale}`)
   - Returns an object with these fields (using `.value` from each, defaulting to empty string):
     - `outcome` from key "outcome"
     - `callType` from key "call_type"
     - `followUp` from key "follow_up_action"
     - `callbackRequested` from key "callback_requested"
     - `callbackWhen` from key "callback_when"
     - `emailConfirmed` from key "prospect_email_confirmed"
     - `hasSeenInvite` from key "has_seen_invite"
   - Combines callback: if callbackRequested is "true", returns "Yes - " + callbackWhen, else "No"
   - Returns empty strings for all fields if analysis or data_collection_results is missing
  </action>
  <verify>
    <automated>grep -c "Outcome\|Call Type\|Summary\|Transcript\|Recording\|Follow Up\|Callback\|Email Confirmed\|Seen Invite" elevenlabs-apps-script.js | grep -q "9" && grep -c "getConversationDetail\|formatTranscript\|buildRecordingUrl\|extractDataCollection" elevenlabs-apps-script.js | grep -q "[4-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CALL_TRACKING_COLUMNS contains exactly 14 entries (5 existing + 9 new)
    - getConversationDetail function exists and calls elevenlabsGet with /convai/conversations/ path
    - formatTranscript function exists and produces "Agent: ...\nUser: ..." format
    - buildRecordingUrl function exists and returns CONFIG.API_BASE + /convai/conversations/{id}/audio
    - extractDataCollection function exists and reads .value from each data_collection_results key
  </acceptance_criteria>
  <done>All 4 helper functions exist, CALL_TRACKING_COLUMNS has 14 entries, no syntax errors in file</done>
</task>

<task type="auto">
  <name>Task 2: Enhance updateSheetWithCallResults to fetch and write full conversation detail</name>
  <files>elevenlabs-apps-script.js</files>
  <read_first>elevenlabs-apps-script.js lines 326-379 (current updateSheetWithCallResults), lines 301-321 (checkBatchStatus), lines 488-506 (pollBatchStatus)</read_first>
  <action>
Rewrite the body of `updateSheetWithCallResults(batchId, apiKey)` (lines 326-379) to:

1. Keep existing setup: get sheet, headers, phoneColIdx, callStatusIdx, callDurationIdx, callNotesIdx, callBatchIdx (lines 326-336 unchanged).

2. Add new column index lookups after the existing ones:
   ```
   var outcomeIdx = headers.indexOf('Outcome');
   var callTypeIdx = headers.indexOf('Call Type');
   var summaryIdx = headers.indexOf('Summary');
   var transcriptIdx = headers.indexOf('Transcript');
   var recordingIdx = headers.indexOf('Recording');
   var followUpIdx = headers.indexOf('Follow Up');
   var callbackIdx = headers.indexOf('Callback');
   var emailConfIdx = headers.indexOf('Email Confirmed');
   var seenInviteIdx = headers.indexOf('Seen Invite');
   ```

3. Keep existing phone-to-row mapping logic (lines 338-352 unchanged).

4. Keep existing batch conversations fetch (line 357). In the per-conversation loop, after matching to a sheet row:

5. For each matched conversation, check if this row already has a transcript (skip detail fetch if transcriptIdx !== -1 and cell is non-empty — avoids re-fetching on subsequent polls):
   ```
   var existingTranscript = transcriptIdx !== -1 ? sheet.getRange(sheetRow, transcriptIdx + 1).getValue() : '';
   if (existingTranscript) continue; // already processed
   ```
   Wait -- this would skip status updates. Instead: always update status/duration, but only fetch detail if transcript cell is empty.

6. Write existing fields (status, duration, notes) as before.

7. Then check if detail is already fetched (transcript cell non-empty). If empty AND conv.status is 'done' or 'completed':
   - Get conversation_id from `conv.conversation_id || conv.id`
   - Call `getConversationDetail(conversationId, apiKey)`
   - If detail is null, skip (log and continue)

8. From the detail response, write each new column:
   - **Summary**: `detail.analysis.transcript_summary || ''` -> summaryIdx
   - **Transcript**: `formatTranscript(detail.transcript)` -> transcriptIdx
   - **Recording**: If `detail.has_audio`, set a hyperlink formula: `=HYPERLINK("url", "Play Recording")` where url = buildRecordingUrl(conversationId). Write via `setFormula()`. If no audio, write empty string.
   - **Data collection fields**: Call `extractDataCollection(detail.analysis)` and write:
     - dc.outcome -> outcomeIdx
     - dc.callType -> callTypeIdx
     - dc.followUp -> followUpIdx
     - dc.callback -> callbackIdx (the combined "Yes - next Tuesday" or "No" string)
     - dc.emailConfirmed -> emailConfIdx
     - dc.hasSeenInvite -> seenInviteIdx

9. Each setValue/setFormula call must check the column index !== -1 before writing.

10. Wrap the detail fetch section in try/catch so one failed conversation does not block the rest. Log errors with Logger.log.

11. Also update the `handleCallWebhook(data)` function (line 433) to look up and write the new columns if the webhook payload contains the relevant fields (conversation_id). If `data.conversation_id` is present, fetch detail and write the same way. This is opportunistic — the main path is through pollBatchStatus.
  </action>
  <verify>
    <automated>grep -c "getConversationDetail\|formatTranscript\|extractDataCollection\|buildRecordingUrl\|outcomeIdx\|callTypeIdx\|summaryIdx\|transcriptIdx\|recordingIdx\|followUpIdx\|callbackIdx\|emailConfIdx\|seenInviteIdx" elevenlabs-apps-script.js | grep -q "[1-9][0-9]*" && grep "HYPERLINK" elevenlabs-apps-script.js | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - updateSheetWithCallResults calls getConversationDetail for each completed conversation
    - All 9 new column indices are looked up via headers.indexOf
    - Transcript is written via formatTranscript()
    - Recording is written as HYPERLINK formula via setFormula()
    - Data collection fields are extracted via extractDataCollection()
    - Already-processed rows (non-empty transcript) skip the detail fetch
    - Each column write checks index !== -1
    - Per-conversation errors are caught and logged, not thrown
  </acceptance_criteria>
  <done>updateSheetWithCallResults fetches full conversation detail for completed calls and populates all 9 new columns; handleCallWebhook also writes detail when conversation_id is present</done>
</task>

</tasks>

<verification>
1. `grep 'CALL_TRACKING_COLUMNS' elevenlabs-apps-script.js` shows array with 14 column names
2. `grep 'getConversationDetail' elevenlabs-apps-script.js` shows function definition and calls
3. `grep 'formatTranscript' elevenlabs-apps-script.js` shows function definition and calls
4. `grep 'extractDataCollection' elevenlabs-apps-script.js` shows function definition and calls
5. `grep 'HYPERLINK' elevenlabs-apps-script.js` shows recording link formula
6. No syntax errors: `node --check elevenlabs-apps-script.js` passes (basic syntax, not GAS-specific)
</verification>

<success_criteria>
- CALL_TRACKING_COLUMNS has 14 entries (5 original + 9 new)
- updateSheetWithCallResults fetches GET /v1/convai/conversations/{id} per completed call
- 9 new columns populated: Outcome, Call Type, Summary, Transcript, Recording, Follow Up, Callback, Email Confirmed, Seen Invite
- Transcript formatted as "Agent: ...\nUser: ..." multi-line string
- Recording column contains HYPERLINK formula
- Data collection values extracted from analysis.data_collection_results
- Already-processed rows (with transcript) are not re-fetched
- Per-call errors do not block other calls
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-and-sheet-integration-fetch-transcripts-recordings-summaries-success-failure-status-per-call-clean-ui-in-sheet/05-01-SUMMARY.md`
</output>
