---
phase: 09-operational-features
plan: 03
type: execute
wave: 1
depends_on: ["09-01", "09-02"]
files_modified:
  - src/campaign.js
  - public/index.html
  - public/js/app.js
autonomous: true
gap_closure: true
requirements: [OPS-02, OPS-03]

must_haves:
  truths:
    - "campaignStartTime is accessible in the finally block so appendHistory records duration"
    - "Dashboard shows delay min/max input fields in Campaign Settings section"
    - "startCampaign POST body includes delayMin and delayMax from the input fields"
  artifacts:
    - path: "src/campaign.js"
      provides: "campaignStartTime declared before try block"
      contains: "const campaignStartTime"
    - path: "public/index.html"
      provides: "Delay min/max number inputs"
      contains: "delay-min"
    - path: "public/js/app.js"
      provides: "delayMin/delayMax in POST body"
      contains: "delayMin"
  key_links:
    - from: "public/js/app.js"
      to: "/api/campaign/start"
      via: "delayMin/delayMax in JSON body"
      pattern: "delayMin.*delayMax"
---

<objective>
Fix the campaignStartTime scoping bug that silently prevents history duration from being saved, and add delay min/max configuration fields to the dashboard so operators can control inter-lead delay timing.

Purpose: Close verification gaps for OPS-02 (delay config not exposed) and OPS-03 (history save broken by scoping bug).
Output: Working delay configuration in dashboard, reliable history persistence with duration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-operational-features/09-CONTEXT.md
@.planning/phases/09-operational-features/09-01-SUMMARY.md
@.planning/phases/09-operational-features/09-02-SUMMARY.md
@.planning/phases/09-operational-features/09-VERIFICATION.md

@src/campaign.js
@public/index.html
@public/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix campaignStartTime scoping bug and add delay config inputs</name>
  <files>src/campaign.js, public/index.html</files>
  <action>
**Bug fix in src/campaign.js (OPS-03):**
Move `const campaignStartTime = Date.now();` from inside the `try` block (currently line 198) to BEFORE the `try` block -- place it right after the template normalization block (after the closing brace of `const tpl = {...}` around line 195). This makes it accessible in the `finally` block where `appendHistory()` references it (line 553). The variable must remain `const` and use `Date.now()`.

Verify the fix by confirming `campaignStartTime` is in the same scope as the `finally` block (function scope of `startCampaign`, not `try` block scope).

**Delay config inputs in public/index.html (OPS-02, per D-08):**
In the Campaign Settings section (section 3, after the daily-limit input around line 55), add two number inputs for delay configuration:

```html
<div style="display:flex; gap:12px; margin-top:4px">
  <div style="flex:1">
    <label for="delay-min">Min Delay (seconds)</label>
    <input type="number" id="delay-min" value="8" min="1" max="60" />
  </div>
  <div style="flex:1">
    <label for="delay-max">Max Delay (seconds)</label>
    <input type="number" id="delay-max" value="15" min="1" max="120" />
  </div>
</div>
<p style="font-size:0.75rem; color:#8b949e; margin:-12px 0 8px 0">Random delay between lead actions. Default: 8-15 seconds.</p>
```

Place this right after the daily-limit input and before the open-profile-toggle div.
  </action>
  <verify>
    <automated>grep -n "const campaignStartTime" src/campaign.js | head -3 && grep -n "delay-min\|delay-max" public/index.html | head -5</automated>
  </verify>
  <done>campaignStartTime is declared before the try block (same function scope as finally). Two number inputs with ids "delay-min" and "delay-max" exist in the Campaign Settings section with defaults 8 and 15.</done>
</task>

<task type="auto">
  <name>Task 2: Wire delay inputs into startCampaign POST body</name>
  <files>public/js/app.js</files>
  <action>
In the `startCampaign()` function in public/js/app.js (around line 204), add reading of delay values and include them in the POST body.

After the `dailyLimit` parsing (around line 209), add:
```javascript
const delayMin = parseInt(document.getElementById('delay-min').value, 10) || 8;
const delayMax = parseInt(document.getElementById('delay-max').value, 10) || 15;
if (delayMin < 1 || delayMax < delayMin) { alert('Delay min must be >= 1 and max must be >= min.'); return; }
```

In the `JSON.stringify` body of the fetch call (around line 226), add `delayMin, delayMax` to the object being sent:
```javascript
body: JSON.stringify({ profileIds: selectedProfileIds, sheetUrl, templates, dailyLimit, mode, messageOpenProfiles: !!document.getElementById('open-profile-msg')?.checked, delayMin, delayMax }),
```

This ensures the backend receives the operator's delay preferences instead of always using defaults.
  </action>
  <verify>
    <automated>grep -n "delayMin\|delayMax" public/js/app.js | head -10</automated>
  </verify>
  <done>startCampaign() reads delay-min and delay-max input values, validates them, and includes delayMin/delayMax in the POST /api/campaign/start body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard -> API | Delay values from user input sent to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | T (Tampering) | delay-min/delay-max inputs | mitigate | Client-side validation (min >= 1, max >= min). Server already applies Number() coercion with undefined fallback to defaults (server.js line 103-104). |
</threat_model>

<verification>
1. `grep -n "const campaignStartTime" src/campaign.js` -- should show declaration BEFORE the `try` keyword
2. `grep -n "delay-min" public/index.html` -- should return input elements
3. `grep -n "delayMin" public/js/app.js` -- should show parsing and inclusion in POST body
4. Manual: Open dashboard, see delay min/max fields with defaults 8/15 in Campaign Settings section
</verification>

<success_criteria>
- campaignStartTime scoping bug is fixed -- history entries will now include correct duration
- Dashboard shows delay min/max fields with sensible defaults (8/15)
- Starting a campaign from the dashboard sends custom delay values to the backend
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-features/09-03-SUMMARY.md`
</output>
