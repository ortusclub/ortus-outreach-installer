---
phase: 11
plan: 05
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
files_modified:
  - .planning/phases/11-check-dms/11-VALIDATION.md
autonomous: false
requirements:
  - DMS-01
  - DMS-02
  - DMS-03
  - DMS-04
  - DMS-05
  - DMS-06
  - DMS-07

must_haves:
  truths:
    - "Full test suite (`node --test tests/**/*.test.js`) exits 0"
    - "Apps Script (`google-apps-script.js`) has been pasted into the Apps Script editor attached to the operator's test sheet"
    - "Manual end-to-end scan against a real GoLogin profile (e.g. 'Antonio') surfaces at least one known reply in the Replies panel AND writes Reply/Reply At/Reply Preview to the sheet"
    - "Re-running Check DMs with no new inbound activity shows an empty Replies panel — watermark advance is functioning"
    - "DMS-06 manual check confirmed: Open Thread opens in the SYSTEM browser, not inside the Electron window"
    - "An error case is exercised (e.g. point at a profile without LinkedIn session) — the Replies panel shows the error tile, watermark NOT advanced, operator can retry"
    - "`.planning/phases/11-check-dms/11-VALIDATION.md` frontmatter is updated: `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`"
    - "`.planning/phases/11-check-dms/11-VALIDATION.md` Per-Task Verification Map is populated — every DMS-XX requirement maps to either an automated command or the manual step"
  artifacts:
    - path: ".planning/phases/11-check-dms/11-VALIDATION.md"
      provides: "Final validation sign-off: status=approved, nyquist_compliant=true, per-task map filled in"
      min_lines: 100
  key_links:
    - from: "11-VALIDATION.md Per-Task Verification Map"
      to: "Every DMS-XX requirement"
      via: "one row per requirement"
      pattern: "DMS-0[1-7]"
---

<objective>
Wave 3 — Close the loop. Run the full test suite, execute the end-to-end manual verification against a real LinkedIn session, confirm DMS-06 works as designed, and update `11-VALIDATION.md` with the final sign-off.

Purpose: Plans 01-04 built the feature. This plan is the phase gate — no one can call Phase 11 "done" until a real reply round-trips from LinkedIn → Replies panel → sheet, AND the Open Thread button provably opens in the system browser. Automated tests can't prove the Electron-specific DMS-06 behavior; a human must click it.

Output:
- Full test suite green
- One real end-to-end run verified by the operator
- `11-VALIDATION.md` updated with completed sign-off
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-check-dms/11-VALIDATION.md
@.planning/phases/11-check-dms/11-01-SUMMARY.md
@.planning/phases/11-check-dms/11-02-SUMMARY.md
@.planning/phases/11-check-dms/11-03-SUMMARY.md
@.planning/phases/11-check-dms/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Run the full test suite; record output</name>
  <read_first>
    - package.json (confirm `test` script still exists and points at `node --test tests/**/*.test.js`)
    - tests/ (confirm 7 test files exist — 5 check-dms + 2 ui)
  </read_first>
  <action>
    Run `npm test` (or the equivalent `node --test tests/**/*.test.js`). Expected outcome: EXIT 0 with every test file passing. If any test fails:
    1. Do NOT advance to Task 2.
    2. Diagnose the failure. If it's in a pure function (check-dms-*.test.js), the regression is in Plan 02 — fix the module.
    3. If it's in a UI test, the regression is in Plan 04 — fix the renderer or HTML.
    4. Re-run. Iterate until GREEN.

    Capture the output (pass/fail counts + any warnings) and save it into `.planning/phases/11-check-dms/11-05-test-output.txt` for the summary.
  </action>
  <verify>
    <automated>node --test tests/**/*.test.js 2>&1 | tee .planning/phases/11-check-dms/11-05-test-output.txt; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/11-check-dms/11-05-test-output.txt` succeeds
    - `grep -E "# (pass|fail) " .planning/phases/11-check-dms/11-05-test-output.txt` shows `# pass` lines
    - `grep "# fail" .planning/phases/11-check-dms/11-05-test-output.txt` shows `# fail 0` (or equivalent)
    - Exit code of the node --test run was 0
  </acceptance_criteria>
  <done>Full test suite green; output archived.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — End-to-end manual verification (Electron dev build)</name>
  <what-built>
    Claude has:
    - Built + tested the Check DMs module (Plan 02)
    - Wired 4 new Express endpoints + extended Apps Script config (Plan 03)
    - Built the Check DMs button + Replies panel + CSS (Plan 04)
    - Confirmed all 7 automated test files pass (Task 1 of this plan)

    What's left is exactly what automated tests cannot prove: that the full Electron app + real GoLogin profile + real LinkedIn session + real Google Sheet stack produces the right behavior end-to-end.
  </what-built>
  <how-to-verify>
    Operator must personally do all of the following — in order — on the Electron dev build:

    **Pre-flight (one-time setup):**
    1. Paste the updated `google-apps-script.js` into the Apps Script editor attached to your test sheet. Save. HEAD deployment means no redeploy needed.
    2. Ensure at least one row in the sheet has: `Message="sent"`, `Account Used="<your-profile-name-e.g.-Antonio>"`, a valid LinkedIn URL for a prospect who has ACTUALLY sent you a new DM reply on LinkedIn. If you need to fake this: pick any connection you've DM'd recently and have them reply.

    **Run 1 — Happy path (DMS-01, DMS-02, DMS-03, DMS-04, DMS-05, DMS-07 integration):**
    3. `npm run electron:dev`
    4. Sign in to the dashboard.
    5. Select the profile(s) from the GoLogin list (same profile name as in step 2's `Account Used`).
    6. Enter the test sheet URL in the Sheet URL field.
    7. Observe the "Check DMs" button in the Campaign section. VERIFY it is present and styled to match the rest of the dashboard (monochrome, hairline border, NOT a loud color).
    8. Click "Check DMs".
    9. VERIFY the button text changes to "Checking DMs — <profile>…" while polling (button.disabled = true).
    10. Wait up to ~60s for the scan to complete (profile launch + warmup + Voyager call).
    11. VERIFY the Replies panel becomes visible and contains the expected reply(ies). Each row shows: prospect name, snippet (first ~100 chars of the reply), relative time, and an "Open Thread" button.
    12. VERIFY the sheet (refresh browser tab) now has `Reply=yes`, `Reply At=<ISO timestamp>`, `Reply Preview=<first ~100 chars>` in the matched row. The 3 new columns should have been auto-added by Apps Script (DMS-04).

    **DMS-06 — Open Thread must open in system browser:**
    13. In the Replies panel, click "Open Thread" on a reply row.
    14. VERIFY the LinkedIn conversation URL opens in your system default browser (Safari/Chrome/Firefox — whichever is set as default).
    15. VERIFY it did NOT open in a new Electron window, an iframe, or inside the existing dashboard window. You should see the LinkedIn conversation with your own normal browser session cookies.
    16. If the URL opens inside Electron: Plan 04 is broken — the `<a target="_blank">` + `setWindowOpenHandler` wiring failed. Return with "❌ DMS-06 failed — opened in Electron."

    **Run 2 — Delta semantics (DMS-07):**
    17. Without any new inbound DMs, click "Check DMs" again.
    18. VERIFY the Replies panel is empty (or shows "No new replies.") — the watermark advanced, so previously surfaced replies do not appear again.
    19. VERIFY the sheet row still has `Reply=yes` (non-destructive — no overwrite).

    **Run 3 — Error surfacing (DMS-03 fallback path; Pitfall 6):**
    20. Intentionally break auth: log OUT of LinkedIn inside the GoLogin profile (leave the profile open, just sign out).
    21. Click "Check DMs".
    22. VERIFY: the Replies panel shows an error tile BEFORE any reply rows — e.g. "Check DMs hit N error(s). Retry in 10–15 min. Watermark NOT advanced."
    23. VERIFY: the watermark did NOT advance — you can confirm by manually inspecting `check-dms-state.json` (path: `app.getPath('userData')/data/check-dms-state.json` — Electron developer console or Finder).

    **Mutex (Pitfall 5):**
    24. With a campaign started (any profile), click Check DMs.
    25. VERIFY you see an error: "Cannot check DMs while a campaign is running" (or the status code 409 surfaced via the alert).
    26. Stop the campaign. Start Check DMs. While it's running, try to start a campaign.
    27. VERIFY you see: "Cannot start campaign while Check DMs is running".

    If any of the above fails: return with "❌ <step N> failed: <what happened>" and Claude will route back to the relevant plan for a fix.
  </how-to-verify>
  <resume-signal>Reply "approved" when all steps above pass. Reply "failed: <step N>: <reason>" otherwise.</resume-signal>
  <action>
    Claude's role in this task is to:
    1. Confirm the automated test suite is green (Task 1 of this plan already ran it).
    2. Confirm all 5 plans (11-01 through 11-04) have SUMMARY files committed — if any summary is missing, block on it.
    3. Pause for the operator to run the end-to-end steps in <how-to-verify>.
    4. Once the operator replies with a resume-signal, record the outcome in `.planning/phases/11-check-dms/11-05-e2e-log.md` (one line per numbered step: PASS/FAIL + notes).
    The core verification itself is manual — see <how-to-verify>.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-check-dms/11-01-SUMMARY.md && test -f .planning/phases/11-check-dms/11-02-SUMMARY.md && test -f .planning/phases/11-check-dms/11-03-SUMMARY.md && test -f .planning/phases/11-check-dms/11-04-SUMMARY.md && test -f .planning/phases/11-check-dms/11-05-e2e-log.md && grep -l "approved" .planning/phases/11-check-dms/11-05-e2e-log.md > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Operator confirms via resume-signal that steps 7-27 above all pass
    - Happy path (Run 1) surfaced at least one real reply in panel + sheet
    - Open Thread confirmed to open in system browser
    - Second run (Run 2) showed empty panel — watermark advances
    - Error path (Run 3) showed error tile, did not advance watermark
    - Both mutex directions (Run 4) return 409 / error message
  </acceptance_criteria>
  <done>Operator has signed off on the full end-to-end behavior, including the manual-only DMS-06 check.</done>
</task>

<task type="auto">
  <name>Task 3 — Update 11-VALIDATION.md with final sign-off</name>
  <read_first>
    - .planning/phases/11-check-dms/11-VALIDATION.md (current state — frontmatter status=draft, nyquist_compliant=false, wave_0_complete=false)
    - .planning/phases/11-check-dms/11-05-test-output.txt (Task 1 output)
    - .planning/phases/11-check-dms/11-01-SUMMARY.md through 11-04-SUMMARY.md (task IDs + test commands to populate the Per-Task Verification Map)
  </read_first>
  <action>
    Edit `.planning/phases/11-check-dms/11-VALIDATION.md` in THREE places:

    1. Frontmatter — flip all three flags:
       ```yaml
       ---
       phase: 11
       slug: check-dms
       status: approved                # was: draft
       nyquist_compliant: true         # was: false
       wave_0_complete: true           # was: false
       created: 2026-04-21
       approved: <today's ISO date>
       ---
       ```

    2. Per-Task Verification Map table — replace the `TBD (populated by planner)` row with actual rows for every task across all 5 plans. Sample (adjust to match the real task IDs + test commands used — use the summaries from Plans 01-04 as the authoritative task list):

       | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
       |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
       | 11-01-T2 | 01 | 0 | DMS-02 stub | — | — | fixture+script | `node --test tests/check-dms-filter.test.js` | ✅ | ✅ green |
       | 11-01-T3 | 01 | 0 | DMS-02/03/04/07 stubs | — | — | unit RED | `node --test tests/check-dms-*.test.js` (expects non-zero) | ✅ | ✅ green |
       | 11-01-T4 | 01 | 0 | DMS-01/05 stubs | — | — | UI smoke RED | `node --test tests/ui/*.test.js` (expects non-zero) | ✅ | ✅ green |
       | 11-02-T1 | 02 | 1 | DMS-03 infra | — | auth cookies stay in-page | unit | `node -e "import('./src/linkedin/helpers.js').then(m=>console.log(!!m.getConversationsPage))"` | ✅ | ✅ green |
       | 11-02-T2 | 02 | 1 | DMS-04 infra | — | non-destructive read-before-write | unit | `node -e "import('./src/sheets-writer.js').then(m=>console.log(!!m.getSheetRowStatus))"` | ✅ | ✅ green |
       | 11-02-T3 | 02 | 1 | DMS-02/03/04/07 | — | untrusted input handled via textContent downstream | unit | `node --test tests/check-dms-*.test.js` | ✅ | ✅ green |
       | 11-02-T4 | 02 | 1 | DMS-01/03/04/07 | — | atomic watermark on success only | integration (manual in Task 2 of this plan) | E2E Run 1 + Run 2 | ✅ | ✅ green |
       | 11-03-T1 | 03 | 2 | DMS-04 | — | ACTION_COLUMNS excludes Reply (empty ≠ '—') | config | `grep "'Reply'" google-apps-script.js` | ✅ | ✅ green |
       | 11-03-T2 | 03 | 2 | DMS-01 | — | mutex prevents concurrent runs | unit+smoke | `node --check server.js` + Run 4 mutex | ✅ | ✅ green |
       | 11-04-T1 | 04 | 2 | DMS-05 | — | textContent for LinkedIn-sourced text | UI smoke | `node --test tests/ui/replies-panel.test.js` | ✅ | ✅ green |
       | 11-04-T2 | 04 | 2 | DMS-01 | — | — | UI smoke | `node --test tests/ui/check-dms-button.test.js` | ✅ | ✅ green |
       | 11-04-T3 | 04 | 2 | DMS-05 cosmetic | — | — | manual | visual inspection in Run 1 step 7 | ✅ | ✅ green |
       | 11-05-T2 | 05 | 3 | DMS-06 (manual-only) | — | system browser owns auth | manual | Run 1 step 13-16 | N/A | ✅ green |

       Remove the existing placeholder row. Keep the Status legend line.

    3. Validation Sign-Off checklist (near the bottom) — flip every checkbox:
       ```markdown
       ## Validation Sign-Off

       - [x] All tasks have `<automated>` verify or Wave 0 dependencies
       - [x] Sampling continuity: no 3 consecutive tasks without automated verify
       - [x] Wave 0 covers all MISSING references
       - [x] No watch-mode flags
       - [x] Feedback latency < 10s
       - [x] `nyquist_compliant: true` set in frontmatter

       **Approval:** approved <today>
       ```
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/11-check-dms/11-VALIDATION.md','utf8'); const checks=[['approved status',/status:\\s*approved/], ['nyquist compliant',/nyquist_compliant:\\s*true/], ['wave 0 complete',/wave_0_complete:\\s*true/], ['DMS-01 row',/DMS-01/], ['DMS-02 row',/DMS-02/], ['DMS-03 row',/DMS-03/], ['DMS-04 row',/DMS-04/], ['DMS-05 row',/DMS-05/], ['DMS-06 row',/DMS-06/], ['DMS-07 row',/DMS-07/], ['approval',/Approval:\\s*approved/]]; let fail=false; for (const [label,re] of checks) { if (!re.test(s)) { console.error('MISSING:', label); fail=true; } } if (/TBD \(populated by planner\)/.test(s)) { console.error('TBD placeholder still present'); fail=true; } process.exit(fail?1:0);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "status: approved" .planning/phases/11-check-dms/11-VALIDATION.md` matches
    - `grep -n "nyquist_compliant: true" .planning/phases/11-check-dms/11-VALIDATION.md` matches
    - `grep -n "wave_0_complete: true" .planning/phases/11-check-dms/11-VALIDATION.md` matches
    - All 7 DMS-XX requirements appear at least once in the Per-Task Verification Map
    - `grep -c "TBD (populated by planner)" .planning/phases/11-check-dms/11-VALIDATION.md` returns 0
    - `grep -c "Approval:" .planning/phases/11-check-dms/11-VALIDATION.md` returns a match ending in `approved`
    - Every "- [ ]" in the Sign-Off section is flipped to "- [x]"
  </acceptance_criteria>
  <done>VALIDATION.md signed off: status=approved, nyquist=true, wave 0 complete, per-task map filled, all requirements accounted for.</done>
</task>

</tasks>

<verification>
- `node --test tests/**/*.test.js` — exits 0
- `grep -n "status: approved" .planning/phases/11-check-dms/11-VALIDATION.md` — matches
- Operator signed off on Task 2 end-to-end run
</verification>

<success_criteria>
Phase 11 is complete when:
- All automated tests pass
- Operator has confirmed end-to-end behavior against a real LinkedIn session + GoLogin profile + Google Sheet
- DMS-06 confirmed by human click (Open Thread → system browser)
- `11-VALIDATION.md` updated: status=approved, nyquist_compliant=true, wave_0_complete=true
- Per-task verification map populated for all 7 DMS requirements
</success_criteria>

<output>
After completion, create `.planning/phases/11-check-dms/11-05-SUMMARY.md` summarising:
- Test suite result (pass/fail counts from `11-05-test-output.txt`)
- Real-world outcome: how many replies surfaced, any schema surprises, any DOM-fallback triggers observed
- Operator's qualitative notes from Task 2 (e.g. "scan took 45s for one profile with ~10 candidates; felt fine")
- Any follow-up items that belong in seeds (auto-responder noise? full-thread fetch demand?)
</output>
