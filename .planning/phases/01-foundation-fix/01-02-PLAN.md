---
phase: 01-foundation-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DASH-01, VARS-01, VARS-02, VARS-03]
user_setup:
  - service: google-apps-script
    why: "Deploy fixed code via browser paste"
    dashboard_config:
      - task: "Open Apps Script editor and paste code"
        location: "https://script.google.com/u/0/home/projects/1FxYM43Yi-OMXuFOwiYCPKs-tKs0BaUG2IzrBo3AVlvnnl_TFuYwVrxPL/edit"
  - service: elevenlabs-dashboard
    why: "Enable TTS override toggle"
    dashboard_config:
      - task: "Toggle TTS override in agent Security settings"
        location: "ElevenLabs Dashboard > Agents > agent_5601kmzey4mve8pswpwvmhckcgnr > Security tab"

must_haves:
  truths:
    - "Fixed Apps Script code is live in the Google Apps Script editor"
    - "TTS override is enabled in the ElevenLabs agent Security settings"
    - "A test batch call with 1 recipient receives all 14 dynamic variables and the agent greets with personalized details"
  artifacts:
    - path: "Google Apps Script editor (remote)"
      provides: "Live code with correct dynamic_variables nesting at lines 182-201"
      contains: "conversation_initiation_client_data"
    - path: "ElevenLabs agent Security settings (remote)"
      provides: "TTS override toggle enabled"
  key_links:
    - from: "Sidebar form inputs"
      to: "conversation_initiation_client_data.dynamic_variables"
      via: "submitBatchCall() in Apps Script"
      pattern: "dynamic_variables"
    - from: "dynamic_variables in API payload"
      to: "Agent prompt {{variable}} references"
      via: "ElevenLabs agent runtime"
      pattern: "\\{\\{prospect_name\\}\\}"
---

<objective>
Deploy the fixed Apps Script code, enable TTS override in the dashboard, and verify with a real test call.

Purpose: The existing code in `elevenlabs-apps-script.js` already has the correct `conversation_initiation_client_data.dynamic_variables` nesting (lines 182-201). It needs to be deployed to the live Apps Script editor. TTS override must be enabled in the agent Security settings as a prerequisite for Phase 2 voice selection.

Output: Fixed code live in Apps Script editor. TTS override enabled. Test call confirms variables work end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fix/01-CONTEXT.md
@.planning/phases/01-foundation-fix/01-RESEARCH.md
@elevenlabs-apps-script.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy fixed Apps Script code via browser paste</name>
  <files>elevenlabs-apps-script.js</files>
  <read_first>
    - elevenlabs-apps-script.js (the complete source to be pasted — verify lines 182-201 have the correct dynamic_variables nesting)
    - .planning/phases/01-foundation-fix/01-CONTEXT.md (D-01: browser paste method, D-02: HEAD deployment means save = live)
  </read_first>
  <action>
    The fixed Apps Script code exists locally in `elevenlabs-apps-script.js` with correct `conversation_initiation_client_data.dynamic_variables` nesting at lines 182-201. All 14 variables (caller_name, host_name, host_first_name, event_name, event_date, event_time, event_city, event_area, event_venue, event_format, event_context, target_audience, prospect_name, prospect_email) are correctly mapped.

    Human must deploy the code to the Apps Script editor (per D-01):

    1. Open the Apps Script editor: https://script.google.com/u/0/home/projects/1FxYM43Yi-OMXuFOwiYCPKs-tKs0BaUG2IzrBo3AVlvnnl_TFuYwVrxPL/edit
    2. In the editor, press Cmd+A to select ALL existing code
    3. Press Delete/Backspace to clear the editor
    4. Copy the ENTIRE contents of `elevenlabs-apps-script.js` from this repo
    5. Paste into the editor (Cmd+V)
    6. Wait for the editor to finish processing (may take a few seconds for ~650 lines)
    7. Press Cmd+S to save
    8. Verify: The editor shows no syntax errors and the code is saved (no unsaved-changes indicator)

    Per D-02: No redeployment step needed — the sidebar uses HEAD deployment, so saving the code makes it live immediately.

    Resume signal: Type "deployed" when the code is saved in the Apps Script editor.
  </action>
  <verify>
    <automated>MANUAL — User confirms code is saved in Apps Script editor with no syntax errors</automated>
  </verify>
  <acceptance_criteria>
    - Apps Script editor contains the code from `elevenlabs-apps-script.js`
    - Lines 182-201 show the `conversation_initiation_client_data: { dynamic_variables: { ... } }` nesting
    - Editor shows no syntax errors
    - File is saved (no unsaved changes indicator)
  </acceptance_criteria>
  <done>Fixed Apps Script code is live in the Google Apps Script editor. VARS-01, VARS-02, VARS-03 deployment complete.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable TTS override in ElevenLabs agent Security settings</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-fix/01-CONTEXT.md (D-07: manual dashboard toggle)
    - .planning/phases/01-foundation-fix/01-RESEARCH.md (Pitfall 1: TTS override is prerequisite for voice override in Phase 2)
  </read_first>
  <action>
    The TTS override toggle needs to be enabled in the ElevenLabs agent Security settings. This is a dashboard-only step that cannot be done via API (per D-07). It is required for Phase 2 voice selection to work — without it, the `conversation_config_override.tts.voice_id` field in the batch payload will be rejected.

    Human must enable TTS override in the ElevenLabs dashboard:

    1. Go to the ElevenLabs dashboard (https://elevenlabs.io/)
    2. Navigate to: Agents > Select agent `agent_5601kmzey4mve8pswpwvmhckcgnr`
    3. Go to the Security tab
    4. Find the TTS override toggle (may be labeled "Allow TTS override" or "Enable client TTS override")
    5. Toggle it ON
    6. Save the settings

    This enables callers (via the API) to override the agent's default TTS voice on a per-call basis using `conversation_config_override.tts.voice_id`.

    Resume signal: Type "enabled" when TTS override is toggled on and saved.
  </action>
  <verify>
    <automated>MANUAL — User confirms TTS override toggle is ON in agent Security settings</automated>
  </verify>
  <acceptance_criteria>
    - TTS override toggle is ON in the agent Security settings
    - Settings are saved in the ElevenLabs dashboard
  </acceptance_criteria>
  <done>TTS override is enabled in the ElevenLabs agent Security settings. DASH-01 complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify with a real test call using all 14 dynamic variables</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-fix/01-CONTEXT.md (D-08: test with 1-recipient batch, D-09: success = agent uses correct personalized details)
    - elevenlabs-apps-script.js (lines 182-201 to understand what variables are sent)
  </read_first>
  <action>
    The fixed code is now live in the Apps Script editor (Task 1), the agent prompt references all 14 variables (Plan 01), and TTS override is enabled (Task 2). Time to verify the full pipeline works end-to-end with a real call (per D-08, D-09).

    Human must test the complete flow:

    1. Open the Google Sheet with the ElevenLabs Calling sidebar
    2. Click the "ElevenLabs Calling" menu > "Open Call Dashboard"
    3. In the sidebar, fill in ALL event details:
       - Caller Name: a test name (e.g., "Sarah")
       - Host Name: a test host (e.g., "John Smith")
       - Host First Name: (e.g., "John")
       - Event Name: (e.g., "Test Event")
       - Event Date: (e.g., "April 15, 2026")
       - Event Time: (e.g., "2:00 PM")
       - Event City: (e.g., "London")
       - Event Area: (e.g., "Mayfair")
       - Event Venue: (e.g., "The Dorchester")
       - Event Format: (e.g., "roundtable")
       - Event Context: (e.g., "AI in Enterprise")
       - Target Audience: (e.g., "CTOs and CIOs")
    4. Ensure the sheet has 1 test row with:
       - A valid phone number (your number) in E.164 format (e.g., +44...)
       - First Name, Last Name, Email columns filled
    5. Submit the batch call (1 recipient only)
    6. Answer the incoming call
    7. Listen for personalized greeting — the agent should use the host name, event name, and prospect name you entered

    SUCCESS criteria per D-09: The agent greets using the correct host name, event name, and other sidebar-entered event details — NOT generic/default greetings.

    Resume signal: Type "verified" if the agent used personalized details, or describe issues if the greeting was generic/wrong.
  </action>
  <verify>
    <automated>MANUAL — User confirms agent greeting includes personalized details from sidebar inputs</automated>
  </verify>
  <acceptance_criteria>
    - Batch call submitted successfully (no API errors)
    - Phone rings with incoming call from the ElevenLabs agent
    - Agent greeting includes personalized details from the sidebar inputs (host name, event name, prospect name)
    - Agent does NOT use generic/placeholder greetings
  </acceptance_criteria>
  <done>Test call confirmed: agent uses personalized details from all 14 sidebar-entered variables. Phase 1 foundation fix is verified end-to-end.</done>
</task>

</tasks>

<verification>
All three tasks complete: code deployed (VARS-01, VARS-02, VARS-03), TTS override enabled (DASH-01), and test call verified with personalized greeting.
</verification>

<success_criteria>
- Fixed Apps Script code is live in the editor with correct `conversation_initiation_client_data.dynamic_variables` nesting
- TTS override is enabled in the ElevenLabs agent Security settings
- A real test call confirms the agent uses personalized details from all 14 sidebar-entered variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fix/01-02-SUMMARY.md`
</output>
