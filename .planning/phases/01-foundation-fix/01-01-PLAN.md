---
phase: 01-foundation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "All 14 dynamic variables are referenced in the agent prompt template with {{variable_name}} syntax"
    - "Agent prompt PATCH preserves all non-prompt fields (llm, temperature, tools)"
  artifacts:
    - path: "API response from GET /v1/convai/agents/agent_5601kmzey4mve8pswpwvmhckcgnr"
      provides: "Current agent prompt text"
      contains: "conversation_config.agent.prompt.prompt"
    - path: "API response from PATCH (if needed)"
      provides: "Updated agent prompt with all 14 variables"
  key_links:
    - from: "Agent prompt template"
      to: "Dynamic variables in batch payload"
      via: "{{variable_name}} syntax matching"
      pattern: "\\{\\{(caller_name|host_name|host_first_name|event_name|event_date|event_time|event_city|event_area|event_venue|event_format|event_context|target_audience|prospect_name|prospect_email)\\}\\}"
---

<objective>
Audit the ElevenLabs agent prompt to verify all 14 dynamic variables are referenced, and PATCH any missing ones in.

Purpose: Dynamic variables passed in the batch payload are silently ignored if the agent prompt does not contain `{{variable_name}}` placeholders. This audit ensures every variable the sidebar sends is actually used by the agent.

Output: Agent prompt confirmed to contain all 14 `{{variable_name}}` references. If any were missing, they have been patched in via the API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fix/01-CONTEXT.md
@.planning/phases/01-foundation-fix/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET agent config and audit prompt for all 14 dynamic variables</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-fix/01-RESEARCH.md (Pattern 2: Agent Prompt Audit via API, Pitfall 3: branch_id required)
    - .planning/phases/01-foundation-fix/01-CONTEXT.md (D-04: list of 14 variables, D-05: API approach, D-06: agent and branch IDs)
  </read_first>
  <action>
    Run a curl command to GET the current agent config with branch_id:

    ```bash
    curl -s -H "xi-api-key: sk_24138756ab4b5a842c6d44cf1851b5536931888de6752303" \
      "https://api.elevenlabs.io/v1/convai/agents/agent_5601kmzey4mve8pswpwvmhckcgnr?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4"
    ```

    Extract the prompt text from `conversation_config.agent.prompt.prompt` in the JSON response.

    Check the prompt text for ALL 14 required variable references using exact `{{variable_name}}` syntax (no spaces inside braces):
    1. `{{caller_name}}`
    2. `{{host_name}}`
    3. `{{host_first_name}}`
    4. `{{event_name}}`
    5. `{{event_date}}`
    6. `{{event_time}}`
    7. `{{event_city}}`
    8. `{{event_area}}`
    9. `{{event_venue}}`
    10. `{{event_format}}`
    11. `{{event_context}}`
    12. `{{target_audience}}`
    13. `{{prospect_name}}`
    14. `{{prospect_email}}`

    Log which variables are FOUND and which are MISSING.

    If ALL 14 are found: Task is complete, no PATCH needed. Proceed to verify step.

    If ANY are missing: Proceed to Task 2 (PATCH).

    IMPORTANT per D-06: Agent ID is `agent_5601kmzey4mve8pswpwvmhckcgnr`, Branch ID is `agtbrch_0801kmzey97dfhwbwgctcmkv4ez4`. Always include `?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4` in the URL to get the branch-specific prompt (Pitfall 3).
  </action>
  <verify>
    <automated>curl -s -H "xi-api-key: sk_24138756ab4b5a842c6d44cf1851b5536931888de6752303" "https://api.elevenlabs.io/v1/convai/agents/agent_5601kmzey4mve8pswpwvmhckcgnr?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4" | python3 -c "import sys,json; p=json.load(sys.stdin)['conversation_config']['agent']['prompt']['prompt']; vars=['caller_name','host_name','host_first_name','event_name','event_date','event_time','event_city','event_area','event_venue','event_format','event_context','target_audience','prospect_name','prospect_email']; missing=[v for v in vars if '{{'+v+'}}' not in p]; print('MISSING:',missing if missing else 'NONE — all 14 found')"</automated>
  </verify>
  <acceptance_criteria>
    - curl GET returns HTTP 200 with JSON containing `conversation_config.agent.prompt.prompt`
    - The prompt text is extracted and logged
    - Each of the 14 variables is checked for `{{variable_name}}` exact match
    - A clear list of FOUND and MISSING variables is produced
  </acceptance_criteria>
  <done>Agent prompt retrieved and audited. Found/missing variable list produced. If all 14 present, plan is complete. If any missing, Task 2 is needed.</done>
</task>

<task type="auto">
  <name>Task 2: PATCH agent prompt to add any missing variable references (conditional — skip if all 14 found)</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-fix/01-RESEARCH.md (Pattern 3: PATCH Agent Config — must send full prompt object; Pitfall 2: PATCH overwrites entire prompt section if partial)
  </read_first>
  <action>
    CONDITIONAL: Only execute this task if Task 1 found missing variables. If all 14 were present, skip entirely.

    If missing variables exist:

    1. Take the FULL `conversation_config.agent.prompt` object from the GET response (not just the prompt string). This object includes `llm`, `temperature`, `tools`, and other fields that MUST be preserved (Pitfall 2).

    2. Modify ONLY the `prompt.prompt` string to add the missing `{{variable_name}}` references. Add them contextually — for example:
       - If `{{prospect_email}}` is missing, add it near where prospect info is referenced
       - If `{{event_area}}` is missing, add it near other event location variables
       - Do NOT just append all missing variables at the bottom — integrate them naturally into the existing prompt

    3. Send a PATCH request with the FULL prompt object:

    ```bash
    curl -s -X PATCH \
      -H "xi-api-key: sk_24138756ab4b5a842c6d44cf1851b5536931888de6752303" \
      -H "Content-Type: application/json" \
      "https://api.elevenlabs.io/v1/convai/agents/agent_5601kmzey4mve8pswpwvmhckcgnr?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4" \
      -d '{"conversation_config":{"agent":{"prompt": FULL_PROMPT_OBJECT_HERE }}}'
    ```

    IMPORTANT: Include `?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4` on the PATCH URL (Pitfall 3). Use direct `UrlFetchApp.fetch()` style — do NOT use the `elevenlabsPost()` helper which hardcodes POST method.

    4. Verify the PATCH response is HTTP 200.

    5. Re-run the GET + audit from Task 1 to confirm all 14 variables are now present.
  </action>
  <verify>
    <automated>curl -s -H "xi-api-key: sk_24138756ab4b5a842c6d44cf1851b5536931888de6752303" "https://api.elevenlabs.io/v1/convai/agents/agent_5601kmzey4mve8pswpwvmhckcgnr?branch_id=agtbrch_0801kmzey97dfhwbwgctcmkv4ez4" | python3 -c "import sys,json; p=json.load(sys.stdin)['conversation_config']['agent']['prompt']['prompt']; vars=['caller_name','host_name','host_first_name','event_name','event_date','event_time','event_city','event_area','event_venue','event_format','event_context','target_audience','prospect_name','prospect_email']; missing=[v for v in vars if '{{'+v+'}}' not in p]; print('ALL 14 PRESENT' if not missing else 'STILL MISSING:'+str(missing)); sys.exit(0 if not missing else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - PATCH response is HTTP 200 (if PATCH was needed)
    - Re-GET of agent config shows all 14 variables present in `{{variable_name}}` syntax
    - The verify command prints "ALL 14 PRESENT" and exits with code 0
    - Non-prompt fields (llm, temperature, tools) are unchanged after PATCH
  </acceptance_criteria>
  <done>Agent prompt contains all 14 dynamic variable references: caller_name, host_name, host_first_name, event_name, event_date, event_time, event_city, event_area, event_venue, event_format, event_context, target_audience, prospect_name, prospect_email. DASH-02 is satisfied.</done>
</task>

</tasks>

<verification>
Run the audit verify command. Output must show "ALL 14 PRESENT" (or "NONE" missing). This confirms DASH-02.
</verification>

<success_criteria>
- All 14 dynamic variables (`caller_name`, `host_name`, `host_first_name`, `event_name`, `event_date`, `event_time`, `event_city`, `event_area`, `event_venue`, `event_format`, `event_context`, `target_audience`, `prospect_name`, `prospect_email`) are referenced in the agent prompt template using `{{variable_name}}` syntax
- Agent prompt non-prompt config (llm, temperature, tools) is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fix/01-01-SUMMARY.md`
</output>
