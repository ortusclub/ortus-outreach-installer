---
phase: 11.3
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/linkedin/check-dms.js
  - src/linkedin/helpers.js
  - src/sheets-writer.js
  - src/campaign.js
  - src/paths.js
autonomous: true
requirements:
  - DMS-01
  - DMS-02
  - DMS-03
  - DMS-04
  - DMS-05
must_haves:
  truths:
    - "src/linkedin/check-dms.js is new and exports checkProfileDms, matchConversationToSheet, shouldWriteReply, performWriteBack, fetchNewConversations, _setDeps (DOM-scrape path DEFERRED per 2026-04-24 revision)"
    - "src/linkedin/helpers.js gains getConversationsPage — additive export only (GraphQL messengerConversations; normalized response shape)"
    - "src/sheets-writer.js gains getSheetRowStatus — additive export only"
    - "src/campaign.js gains 2 named exports (ensureOpen, extractLinkedInUrl — or equivalents) — NO other changes, NO behavior change, batch loop byte-identical"
    - "src/paths.js used unchanged (already exports dataPath)"
    - "All 5 RED tests from Plan 11.3-01 (post-DOM-removal) flip to GREEN after this plan executes"
    - "Baseline test suite remains green (51 pass, 2 skip)"
  artifacts:
    - path: "src/linkedin/check-dms.js"
      provides: "orchestrator + pure functions (matchConversationToSheet, shouldWriteReply, fetchNewConversations, performWriteBack, checkProfileDms) + _setDeps test hook"
    - path: "src/linkedin/helpers.js"
      provides: "getConversationsPage (GraphQL, normalized shape)"
    - path: "src/sheets-writer.js"
      provides: "getSheetRowStatus (additive)"
    - path: "src/campaign.js"
      provides: "2 named exports (additive, no behavior change)"
  key_links:
    - from: "checkProfileDms orchestrator"
      to: "getConversationsPage + matchConversationToSheet + shouldWriteReply + performWriteBack"
      via: "orchestrator wires pure functions + I/O helpers (DOM fallback deferred)"
    - from: "Plan 11.3-01 RED tests"
      to: "Plan 11.3-02 GREEN"
      via: "tests assert against implementations added here"
---

<objective>
Build the core Check DMs module + extend 3 existing files with additive-only changes. When this plan finishes: all 6 RED tests from Wave 0 flip to GREEN, and the orchestrator is callable from the future HTTP endpoints (Wave 2 Plan 03). No UI yet.

Purpose: This is the load-bearing plan. It's also the riskiest because it touches `src/campaign.js` (protected 11.1/11.2 code) — hence the strict "2 lines, additive only" contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/11.3-check-dms/11.3-CONTEXT.md
@.planning/phases/11.3-check-dms/11.3-RESEARCH.md
@.planning/phases/11.3-check-dms/11.3-PATTERNS.md

# Files being modified — read these FULLY before editing
@src/linkedin/helpers.js
@src/linkedin/outreach.js  # reference — not modified; pattern source
@src/sheets-writer.js
@src/campaign.js

# Fixtures from Plan 11.3-01
@tests/fixtures/voyager-conversations-page1.json
@tests/fixtures/inbox-dom-page1.html
@tests/fixtures/sheet-rows.json

# PRESERVATION CONTRACT
See `11.3-CONTEXT.md` <preservation_contract>. The executor MUST:
- Verify `git diff src/campaign.js` after Task 4 shows ONLY the 2 added export lines — no other change
- Verify `git diff src/linkedin/helpers.js` shows ONLY additions below existing code
- Verify `git diff src/sheets-writer.js` shows ONLY additions below existing code
- If ANY existing function body changes: STOP, revert, report.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/linkedin/check-dms.js with pure functions</name>
  <files>src/linkedin/check-dms.js</files>
  <action>
Create the new file with these exports (in this order for readability):

1. **`SALES_NAV_URL_RE` / `IN_URL_RE` / `THREAD_URL_RE` constants** as needed.
2. **`parseInboxDom(html)`** — pure function. Takes HTML string, returns array of `{ participant: {firstName, lastName}, lastMessage, timestamp, threadId }`. Uses jsdom/happy-dom IF devDep available, else regex-based parser (per fixture analysis).
3. **`matchConversationToSheet(conversation, candidateRows)`** — pure function per RESEARCH.md Finding 4. Returns `{ match, reason, candidates? }`.
4. **`shouldWriteReply(currentStatus, newReply)`** — pure predicate. Returns true if `currentStatus.Reply !== 'yes'`.
5. **`performWriteBack(sheetUrl, linkedinUrl, reply, linkedinColumn)`** — calls `getSheetRowStatus` + `shouldWriteReply` + `updateSheetRow`. Exported so Plan 11.3-05 integration test can exercise the real code path (not a re-implementation).
6. **`checkProfileDms(profileId, { watermark, sheetUrl, linkedinColumn, rows })`** — the orchestrator. Opens the profile via `ensureOpen`, calls `getConversationsPage` (Voyager) with Wave 0-verified shape; on non-2xx, falls back to `scrapeInboxDom`; filters by watermark; matches to rows; calls `performWriteBack` for each match; returns `{ replies, errors, ambiguous }`.

NO console.log spam in pure functions. Orchestrator uses `console.log('[check-dms] …')` pattern.

No throwing across module boundary. Errors returned as `{ errors: [...] }`.
  </action>
  <verify>
    <automated>node -c src/linkedin/check-dms.js && node --input-type=module -e "import('./src/linkedin/check-dms.js').then(m => { ['checkProfileDms','parseInboxDom','matchConversationToSheet','shouldWriteReply','performWriteBack'].forEach(f => { if(typeof m[f]!=='function') throw new Error('missing: '+f); }); console.log('all exports ok'); })"</automated>
  </verify>
  <done>
    - 5 required functions exported
    - `node -c` passes
    - No import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend src/linkedin/helpers.js with getConversationsPage + scrapeInboxDom</name>
  <files>src/linkedin/helpers.js</files>
  <action>
Append two new exported async functions at the END of the file (after existing exports):

1. **`getConversationsPage(page, { start = 0, count = 20 } = {})`** — copies the auth scaffolding from `getVoyagerDegree` at line 112 byte-for-byte. Calls `/voyager/api/messaging/conversations?...` (URL per RESEARCH.md Finding 1). Returns parsed JSON or null on error.
2. **`scrapeInboxDom(page)`** — navigates to `https://www.linkedin.com/messaging/`, waits for DOM settle, returns `await page.content()` (raw HTML). `parseInboxDom` in check-dms.js parses this. Alternative: do `page.evaluate` with selectors and return structured data directly (pick one, match what the fixture supports).

**Preservation check:** run `git diff src/linkedin/helpers.js` — must show ONLY additions, no modification to `randomDelay`, `clickByAria`, `clickByText`, `findButtonByText`, `getVoyagerDegree`, `getConnectionStatus`, `personalizeTemplate`.
  </action>
  <verify>
    <automated>node -c src/linkedin/helpers.js && git diff HEAD src/linkedin/helpers.js | grep -E "^-" | grep -vE "^---|^-$" | wc -l | awk '{if($1>0) {print "FAIL: helpers.js has deletions"; exit 1} else print "OK: additive only"}'</automated>
  </verify>
  <done>
    - 2 new exports added
    - Zero lines removed from helpers.js
    - Existing exports untouched
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend src/sheets-writer.js with getSheetRowStatus</name>
  <files>src/sheets-writer.js</files>
  <action>
Append `getSheetRowStatus(sheetUrl, linkedinUrl, linkedinColumn)` — queries the Apps Script webapp for a specific row's `Reply` / `Reply At` / `Reply Preview` columns. Returns `{ Reply, ReplyAt, ReplyPreview }` or `null` if row not found.

Pattern: copy the Apps Script webapp GET request shape from `updateSheetRow` at line 96. Use an `action=get_row` param (the existing webapp already handles similar patterns per Apps Script convention).

NOTE: The actual Apps Script endpoint extension is handled in Plan 11.3-03 Task 3. This task writes the CLIENT-side fetch assuming the endpoint exists.

**Preservation check:** `git diff src/sheets-writer.js` must show ONLY additions.
  </action>
  <verify>
    <automated>node -c src/sheets-writer.js && git diff HEAD src/sheets-writer.js | grep -E "^-" | grep -vE "^---|^-$" | wc -l | awk '{if($1>0) {print "FAIL: deletions"; exit 1} else print "OK"}'</automated>
  </verify>
  <done>
    - 1 new export
    - Zero removals
  </done>
</task>

<task type="auto">
  <name>Task 4: Add 2 named exports to src/campaign.js (HIGH SCRUTINY)</name>
  <files>src/campaign.js</files>
  <action>
Phase 11.3's check-dms module needs `ensureOpen` (to open a profile for scanning) and `extractLinkedInUrl` (to pull the URL from a sheet row) — both currently local functions in campaign.js.

**The minimal change:** Convert local `function checkProfileHealth` → `export function checkProfileHealth` (1 line change: prepend `export `). Same for `function extractLinkedInUrl` → `export function extractLinkedInUrl`. Do NOT touch anything else.

Actually — the current code uses `ensureOpen`, not `checkProfileHealth`. Check what's exported and what check-dms.js actually needs. If `ensureOpen` is what's needed, export that.

**HIGH-SCRUTINY PRESERVATION CHECK:**
```bash
git diff HEAD src/campaign.js
```
Must show EXACTLY 2 new lines added (the two `export` keywords). If diff shows any other change — including trailing whitespace — revert immediately and investigate.

The batch loop, ambient sampler hookup, round-robin round-sleep (fixed 2026-04-23 in commit aa8cdbc), between-batch decision, all helpers — all byte-identical.
  </action>
  <verify>
    <automated>node -c src/campaign.js && git diff HEAD src/campaign.js | grep -c "^+" | awk '{if($1!=3) {print "FAIL: expected exactly 3 +lines (2 code + 1 diff header-ish); got: "$1; exit 1} else print "OK: 2-line export change"}'</automated>
  </verify>
  <done>
    - 2 named exports added
    - No other change
    - `npm test` still 51 pass, 2 skip (no behavior drift)
  </done>
</task>

<task type="auto">
  <name>Task 5: Run RED tests — confirm they flip to GREEN</name>
  <files>(none — test run only)</files>
  <action>
Run the Wave 0 test suite:
```bash
npm run test:check-dms
```

Expected: all 6 files now pass. If any fails:
- Examine the error
- If it's a missing export: go back to Task 1-3 and add
- If it's a fixture mismatch: the implementation diverged from the fixture — fix the implementation (fixture is authoritative per Plan 11.3-01)
- If it's a Voyager auth issue: the `getConversationsPage` implementation deviated from `getVoyagerDegree` — copy more faithfully

Run full suite after:
```bash
npm test
```
Expected: 51 existing pass + 6 new pass + 2 existing skip = 57 pass, 2 skip.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 6 check-dms-*.test.js files pass
    - Full suite: 57 pass, 2 skip, 0 fail
  </done>
</task>

</tasks>

<verification>
Post-execution:
1. `git diff HEAD~5 HEAD -- src/campaign.js` must show ONLY the 2-line export change
2. `git diff HEAD~5 HEAD -- src/linkedin/helpers.js` must show ONLY additions (zero deletions)
3. `git diff HEAD~5 HEAD -- src/sheets-writer.js` must show ONLY additions (zero deletions)
4. `npm test`: 57 pass, 2 skip, 0 fail
5. `node -e "import('./src/linkedin/check-dms.js').then(m => console.log(Object.keys(m)))"` shows all 5 required exports
</verification>

<success_criteria>
- All Wave 0 tests GREEN
- Full suite GREEN
- 4 files modified, all within preservation contract
- campaign.js batch loop byte-identical to HEAD~5
- Module ready for Wave 2's HTTP endpoints
</success_criteria>

<output>
Create `.planning/phases/11.3-check-dms/11.3-02-SUMMARY.md`:
- Line counts for check-dms.js (expected ~300)
- Diff stats for each touched file
- Test pass counts before/after
</output>
