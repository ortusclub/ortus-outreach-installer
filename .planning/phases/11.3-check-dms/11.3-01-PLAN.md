---
phase: 11.3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tests/check-dms-helpers.test.js
  - tests/check-dms-orchestrator.test.js
  - tests/check-dms-matching.test.js
  - tests/check-dms-write-predicate.test.js
  - tests/check-dms-dom-fallback.test.js
  - tests/check-dms-pagination.test.js
  - tests/fixtures/voyager-conversations-page1.json
  - tests/fixtures/voyager-conversations-page1-degraded.json
  - tests/fixtures/voyager-conversations-empty.json
  - tests/fixtures/sheet-rows.json
  - tests/fixtures/inbox-dom-page1.html
  - tests/capture-voyager-fixture.md
autonomous: true
requirements:
  - DMS-01
  - DMS-04
  - DMS-05
  - DMS-06
  - DMS-07
must_haves:
  truths:
    - "Wave 0 creates failing tests that fail ONLY because src/linkedin/check-dms.js doesn't exist yet (TDD RED)"
    - "Live Voyager fixture captured from an authenticated LinkedIn session and checked into tests/fixtures/"
    - "DOM fallback fixture captured from https://www.linkedin.com/messaging/"
    - "Every pure function that Plan 11.3-02 will implement has at least one unit test pointing at it"
    - "No existing tests break (baseline 51 pass, 2 skip must hold)"
    - "No source files outside tests/ are touched"
  artifacts:
    - path: "tests/fixtures/voyager-conversations-page1.json"
      provides: "live Voyager response fixture"
    - path: "tests/fixtures/inbox-dom-page1.html"
      provides: "live DOM fallback fixture"
    - path: "tests/check-dms-helpers.test.js"
      provides: "RED tests for getConversationsPage + scrapeInboxDom"
    - path: "tests/check-dms-matching.test.js"
      provides: "RED tests for matchConversationToSheet (exact, ambiguous, unmatched)"
    - path: "tests/check-dms-write-predicate.test.js"
      provides: "RED tests for shouldWriteReply non-destructive predicate"
  key_links:
    - from: "Wave 0 fixtures"
      to: "Wave 1 implementation in src/linkedin/check-dms.js"
      via: "fixtures are authoritative schema contract — implementation asserts against them"
---

<objective>
Build the validation harness: capture live LinkedIn fixtures + write failing RED tests for every pure function the Wave 1 module will implement. When this plan finishes, no production code has been written, but every downstream plan has an executable contract to assert against.

Purpose: RESEARCH.md identifies one MEDIUM-confidence unknown (Voyager response schema) + one LOW-confidence unknown (DOM fallback selectors). Fixtures freeze both. Without this, Wave 1 would guess the schema and we'd be firefighting in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/11.3-check-dms/11.3-CONTEXT.md
@.planning/phases/11.3-check-dms/11.3-RESEARCH.md
@.planning/phases/11.3-check-dms/11.3-PATTERNS.md

# Preservation contract
See `11.3-CONTEXT.md` <preservation_contract> section. Wave 0 touches NO files outside `tests/` and `package.json`. If the executor finds itself editing `src/`, `public/`, `server.js`, or any existing test file, STOP and report.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live Voyager fixture + document capture procedure</name>
  <files>tests/fixtures/voyager-conversations-page1.json, tests/capture-voyager-fixture.md</files>
  <action>
**This task requires operator action (can't be fully automated):**
1. Operator opens Chrome, logs into LinkedIn with a Sales-Nav-enabled account that has some DMs.
2. Opens DevTools → Network tab → filters for "messaging/conversations".
3. Visits `https://www.linkedin.com/messaging/`.
4. Finds the XHR request to `/voyager/api/messaging/conversations` (or V2 variant).
5. Right-clicks → Copy as fetch → OR "Save response" → pastes/saves the JSON into `tests/fixtures/voyager-conversations-page1.json`.
6. Manually redacts any personal info (replace real names with "Redacted Name 1", "Redacted Name 2"; keep the JSON structure intact).

**Write `tests/capture-voyager-fixture.md`** documenting the above procedure so future regens are reproducible.

**Write a degraded fixture:** copy page1.json → `voyager-conversations-page1-degraded.json`, strip `events` arrays so the implementation is forced to handle missing-events gracefully.

**Write an empty fixture:** `voyager-conversations-empty.json` with `{ "elements": [], "paging": { "count": 20, "start": 0, "total": 0 } }`.
  </action>
  <verify>
    <automated>test -s tests/fixtures/voyager-conversations-page1.json && test -s tests/fixtures/voyager-conversations-page1-degraded.json && test -s tests/fixtures/voyager-conversations-empty.json && test -s tests/capture-voyager-fixture.md</automated>
  </verify>
  <done>
    - Three Voyager fixtures checked in + capture procedure documented
    - Fixtures contain no personal data (names redacted; URLs may stay)
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture DOM fallback fixture</name>
  <files>tests/fixtures/inbox-dom-page1.html</files>
  <action>
With the same LinkedIn session from Task 1:
1. Visit `https://www.linkedin.com/messaging/`.
2. Open DevTools → Elements → find the `<ul>` containing conversation `<li>` items.
3. Right-click the containing element → Copy → Copy outerHTML.
4. Paste into `tests/fixtures/inbox-dom-page1.html`.
5. Redact names (same treatment as Voyager fixture).

If LinkedIn's DOM is React-generated and classes look minified/hashed, note this in the fixture file as an HTML comment so Plan 11.3-02's `scrapeInboxDom` can plan around it.
  </action>
  <verify>
    <automated>test -s tests/fixtures/inbox-dom-page1.html && grep -q "msg-conversation" tests/fixtures/inbox-dom-page1.html</automated>
  </verify>
  <done>
    - DOM fixture captured + names redacted
  </done>
</task>

<task type="auto">
  <name>Task 3: Write sheet-rows test fixture</name>
  <files>tests/fixtures/sheet-rows.json</files>
  <action>
Hand-write a fixture simulating a sheet pre-filter result for a single profile. Include:
- 3 rows with `Message="sent"` and `Account Used="Antonio"` (match candidates)
- 2 rows with `Message="sent"` but `Account Used="Patricia"` (out of scope)
- 1 row pair with the same first+last name (ambiguous case)
- 1 row with Reply="yes" already set (non-destructive test case)

Shape matches the existing sheet.js parser output (array of objects keyed by column name).
  </action>
  <done>
    - sheet-rows.json has at least 6 rows covering exact/ambiguous/unmatched/already-replied cases
  </done>
</task>

<task type="auto">
  <name>Task 4: Write RED tests — helpers + orchestrator + matching + write-predicate + DOM fallback + pagination</name>
  <files>tests/check-dms-*.test.js (six files)</files>
  <action>
Each test file imports from `../src/linkedin/check-dms.js` (which does not exist yet → test RUN fails with `ERR_MODULE_NOT_FOUND`, which is the correct TDD RED signal).

Files + what they test:

1. **`tests/check-dms-helpers.test.js`** — `getConversationsPage(page, { start, count })` (mocks `page.evaluate`, asserts auth headers + URL + returns parsed JSON).
2. **`tests/check-dms-orchestrator.test.js`** — `checkProfileDms(profileId, { watermark, sheetUrl, linkedinColumn })` end-to-end with mocks.
3. **`tests/check-dms-matching.test.js`** — `matchConversationToSheet(conv, rows)` pure function: exact match, ambiguous, unmatched, case/whitespace tolerance.
4. **`tests/check-dms-write-predicate.test.js`** — `shouldWriteReply(currentStatus, newReply)` pure predicate: skips when Reply="yes" already, writes when empty.
5. **`tests/check-dms-dom-fallback.test.js`** — `parseInboxDom(html)` + `scrapeInboxDom(page)` asserting against the DOM fixture.
6. **`tests/check-dms-pagination.test.js`** — pagination stop condition (short-circuit on old messages).

Every test uses `node:test` + `node:assert/strict`. Pattern model: `tests/mac-window.test.js`.

Tests MUST be written to FAIL (RED) when run now, because `src/linkedin/check-dms.js` doesn't exist yet. They flip GREEN when Plan 11.3-02 implements the module.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "fail|pass" | head -5</automated>
  </verify>
  <done>
    - 6 test files created
    - Running `npm test` shows the pre-existing 51 pass, 2 skip, PLUS 6 new failing test files (expected RED state)
    - No existing test breaks
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire npm test:check-dms for quick iteration</name>
  <files>package.json</files>
  <action>
Add a `"test:check-dms"` npm script:
```json
"test:check-dms": "node --test tests/check-dms-*.test.js"
```

Keep `"test"` unchanged (still `node --test tests/*.test.js`).
  </action>
  <verify>
    <automated>grep -q '"test:check-dms"' package.json && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(!p.scripts['test:check-dms']) throw new Error('script missing'); console.log('ok');"</automated>
  </verify>
  <done>
    - `npm run test:check-dms` runs only the 6 new test files (6 fails expected in RED state)
    - `npm test` still runs everything (51 pass + 6 new fails in RED state)
  </done>
</task>

</tasks>

<verification>
After all 5 tasks:
- 6 test files exist under `tests/check-dms-*.test.js`
- 5 fixture files exist under `tests/fixtures/`
- 1 capture-procedure doc exists
- `package.json` has new `test:check-dms` script
- No files outside `tests/` and `package.json` touched (verify: `git diff --name-only` must show exactly those two directories)
- Test suite RED state confirmed: 51 pass, 2 skip, 6 new fails — all 6 fails are because `src/linkedin/check-dms.js` doesn't exist yet
</verification>

<success_criteria>
- Live fixtures captured — reproducible via capture-voyager-fixture.md
- 6 RED tests point at Wave 1's module
- Zero touches outside tests/ and package.json
- Preservation contract honored (no 11.1/11.2 code modified)
</success_criteria>

<output>
Create `.planning/phases/11.3-check-dms/11.3-01-SUMMARY.md` listing the 6 test files + fixture line counts + confirmation of RED state.
</output>
