---
phase: 11.3
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - tests/check-dms-writeback-integration.test.js
  - .planning/phases/11.3-check-dms/11.3-VALIDATION.md
autonomous: false  # requires operator-run manual E2E
requirements:
  - DMS-01
  - DMS-02
  - DMS-03
  - DMS-04
  - DMS-05
  - DMS-06
  - DMS-07
must_haves:
  truths:
    - "Integration test asserts writeback flow end-to-end against the REAL exported performWriteBack from src/linkedin/check-dms.js"
    - "Operator runs a real Check DMs cycle on a live LinkedIn session and sees at least one real reply round-trip: LinkedIn → panel → sheet"
    - "Open Thread button provably opens in system browser (not inside Electron)"
    - "Full test suite: 57 existing pass + 1 new integration test = 58 pass, 2 skip"
    - "11.3-VALIDATION.md updated with final sign-off"
  artifacts:
    - path: "tests/check-dms-writeback-integration.test.js"
      provides: "wire-level test for non-destructive writeback"
    - path: ".planning/phases/11.3-check-dms/11.3-VALIDATION.md"
      provides: "phase sign-off"
  key_links:
    - from: "Plan 04's UI"
      to: "Plan 02's orchestrator"
      via: "HTTP endpoints from Plan 03 + live LinkedIn session"
---

<objective>
Close Phase 11.3. Write one integration test that proves the writeback flow is wire-correct (not just unit-correct). Then the operator runs a real E2E against a live LinkedIn session. Then we update VALIDATION.md and the phase is done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/11.3-check-dms/11.3-CONTEXT.md
@src/linkedin/check-dms.js
@src/sheets-writer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — imports the REAL performWriteBack helper (no re-implementation)</name>
  <files>tests/check-dms-writeback-integration.test.js</files>
  <action>
Write a test that:
1. Imports `performWriteBack` directly from `src/linkedin/check-dms.js`
2. Mocks `updateSheetRow` and `getSheetRowStatus` at the module-boundary (via dependency injection or module stub)
3. Asserts the non-destructive path: when `getSheetRowStatus` returns `{ Reply: 'yes' }`, `updateSheetRow` is NOT called
4. Asserts the writeback path: when `getSheetRowStatus` returns `{ Reply: '' }` or null, `updateSheetRow` IS called with the expected shape

**Why this matters:** a unit test for `shouldWriteReply` (already in Plan 01) tests the predicate in isolation. This test exercises the ORCHESTRATOR's full call chain (getStatus → predicate → updateRow) with the REAL code from check-dms.js. If someone later refactors the call chain wrong, this test catches it; the unit test wouldn't.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "check-dms-writeback-integration" | grep -q "ok" || { echo "integration test failed"; exit 1; }</automated>
  </verify>
  <done>
    - 1 new test, passes, imports from real module
    - Full suite: 58 pass, 2 skip
  </done>
</task>

<task type="manual">
  <name>Task 2: Manual E2E — operator runs Check DMs against a real LinkedIn session</name>
  <files>(browser + electron + live LinkedIn)</files>
  <action>
**Operator steps:**
1. Restart Electron (`npm run electron:dev`).
2. Open dashboard. Select at least one profile that has previously-sent DMs to people on the sheet.
3. Click **Check DMs**.
4. Watch the Replies panel populate.
5. For each reply shown: verify the name + snippet match reality (open LinkedIn directly in a different tab to cross-check).
6. Click **Open Thread** on one of the replies. Verify it opens in the system browser (Chrome/Safari), NOT inside Electron.
7. Open the Google Sheet. Verify the matched rows have:
   - `Reply = "yes"`
   - `Reply At = <timestamp>`
   - `Reply Preview = <first ~100 chars>`
8. Verify rows already marked `Reply = "yes"` (if any) were NOT overwritten.
9. Click **Check DMs** a second time (same profile). Verify the Replies panel now says "No new replies since..." (empty — delta semantics working).

Edge cases to test if accessible:
- Multi-profile select: both profiles' replies appear in the panel, grouped.
- Ambiguous names: if operator has two sheet rows with identical first+last names, verify the "? Ambiguous" marker renders.
- Error path: temporarily break the Voyager endpoint (e.g. block via DevTools) — verify the DOM fallback triggers, OR the panel shows the error line + watermark does NOT advance.

**Operator reports:** YES/NO per item. If any NO → Plan 05 is INCOMPLETE, route fixes through /gsd-quick.
  </action>
  <done>
    - All 9 items verified YES
    - Edge cases spot-checked
  </done>
</task>

<task type="auto">
  <name>Task 3: Update 11.3-VALIDATION.md with final sign-off</name>
  <files>.planning/phases/11.3-check-dms/11.3-VALIDATION.md</files>
  <action>
Update frontmatter:
- `status: complete`
- `wave_0_complete: true`
- `wave_1_complete: true`
- `wave_2_complete: true`
- `wave_3_complete: true`

Add a "Final sign-off" section with:
- Test counts (58 pass, 2 skip, 0 fail)
- Manual E2E results (9/9 pass)
- Commit list for the phase
- Any known issues / follow-ups (link to seeds if we captured any)

Append 11.3 to STATE.md "Phases Completed" or equivalent.
  </action>
  <done>
    - VALIDATION.md reflects completion
    - STATE.md updated
  </done>
</task>

</tasks>

<verification>
- Integration test exists + passes
- Manual E2E signed off by operator
- VALIDATION.md updated
- STATE.md updated
</verification>

<success_criteria>
- Real reply round-trip: LinkedIn → Replies panel → sheet
- "Open Thread" provably opens in system browser
- Delta semantics working (2nd click = empty panel)
- No 11.1 / 11.2 / preset / template regression
- Phase 11.3 status: complete
</success_criteria>
