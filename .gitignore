node_modules/

# Secrets
.env

# Runtime state — per-installation, not source
data/state.json
data/templates.json
data/schedules.json
data/users.json
data/history.json
data/presets.json
data/drained-followups.json
data/.session-secret
data/campaign.log
data/fatal-errors.log
data/local-chrome/
data/local-profile/
data/draft-name.json
data/queued-campaigns.json
data/drafts.json
data/bulk-check-cooldown.json
data/post-campaign-bulk-check.json
data/errors.log.json
data/test-mode.json
data/operator-id.json
data/fg-cloud-runs.json

# Build output
dist/
.vercel

# Local junk
.DS_Store
.playwright-mcp/

# Parallel-work worktrees (per-developer, not source)
.worktrees/
apps-script/

# Superpowers brainstorm visual-companion scratch
.superpowers/

# Team Connections (local-only, PII)
data/connections/
data/connections-cache.json
out/
k8s/fg-roster/secret.yaml
.claude/worktrees/
data/funnel-i/

data/cloud-reconcile.json
# Per-operator Magellan state: memberId -> HubSpot contact id, for the sheet's
# HubSpot Link column. Machine-local, never shared.
data/magellan-hubspot-ids.json
# Live monitoring state — which campaign is being watched and when its next
# auto-check is due. Per-operator runtime state that was tracked by accident:
# every pull overwrote the puller's file with the committer's laptop, so an
# operator could rehydrate a monitoring schedule that was never theirs.
# The app treats an absent file as "nothing monitoring" (readMonitoringState
# returns null, clearMonitoringState deletes it routinely), so untracked is the
# state it is already designed for.
data/monitoring-campaign.json
data/monitoring-campaign.local-backup.json
